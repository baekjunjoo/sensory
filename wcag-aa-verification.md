# Sensory WCAG 2.2 AA 보완·검증 기록

## 범위와 기준

이번 보완은 Sensory의 기존 3D 파스텔 섬·7일 촉각 학습지·Liblouis 점역·Super Dot TTS·DotPad 연결 UI를 유지한 상태에서, **WCAG 2.2 AA의 키보드 사용, 보이는 초점, 반복 콘텐츠 건너뛰기, 시맨틱 구조, 상태 안내, 최소 조작 영역, 감축 모션**에 해당하는 구현을 점검한 기록이다. `axe-core`의 jsdom 검사는 배치와 계산 색상 정보가 없으므로 `color-contrast` 규칙을 제외하고 실행한다. 색 대비와 조작 영역은 실제 브라우저 계산 스타일·화면으로 별도 확인한다.

| 검증 항목 | 현재 확인 결과 | 근거 |
| --- | --- | --- |
| 홈 ARIA·구조·이름 자동 검사 | 통과 | `Home.friendProfile.test.tsx`의 axe 검사 |
| TTS ARIA·구조·이름 자동 검사 | 통과 | `AccessibilityTts.test.tsx`의 axe 검사 |
| 주간 탭 키보드 | 통과 | ArrowLeft·ArrowRight·Home·End로 선택과 초점이 동기화되는 단위 테스트 |
| 반복 콘텐츠 건너뛰기 | 통과 | 개발 브라우저에서 첫 Tab에 링크가 표시되고 Enter 후 `#today`로 이동하는 것을 확인 |
| DotPad 지원 안내 | 통과 | 비활성 연결 버튼이 `dotpad-support-note`를 참조하고 상태 영역이 `role="status"`로 안내됨 |

## 현재 제한

실제 음성 출력과 스페인어 설치 음성 선택은 오디오 출력 장치가 있는 사용자 브라우저에서, 실제 DotPad 320의 BLE·USB 권한과 촉각 프레임 출력은 Chrome 또는 Chromium과 실제 기기에서 각각 확인해야 한다. 이 기록은 해당 물리 장치·음성 결과가 이미 검증되었다는 의미가 아니다.

## 브라우저 측정과 대비 보강

개발 브라우저에서 실제 렌더링한 결과, 친구 선택 버튼은 **46 × 64px**, TTS와 DotPad의 주요 버튼·선택 상자는 **최소 44px 높이**, 선택된 주간 학습지 탭은 **80px 높이**, 답안 카드는 **94px 높이**로 확인되었다. 첫 Tab에서 건너뛰기 링크가 화면 좌상단에 나타나고, Enter 입력 후 URL 해시가 `#today`로 전환되었다.

이미지 밝기에 따라 흰색 글자의 대비가 흔들릴 수 있는 히어로·섹션·리포트 영역은 진청색 반투명 또는 불투명 배경으로 보강했다. 보호자 리포트의 요약·주간·세부 카드는 불투명한 진청색 패널로 통일하고, 답안의 선택·정답·오답 상태는 배경색뿐 아니라 화면의 텍스트·아이콘·상태 안내를 함께 유지한다.

## 상호작용·반응형 화면 확인

개발 브라우저에서 선택된 수요일 탭에 포커스를 둔 뒤 ArrowRight 키를 입력해, 목요일 탭으로 포커스와 선택 상태가 함께 옮겨지고 학습지 제목·답안·라이브 안내가 갱신되는 것을 확인했다. 390 × 844px 화면에서는 TTS·DotPad 제어와 7일 탭, 답안, 모바일 하단 탐색이 세로·2열 배치로 유지되며 수평 겹침 없이 나타나는 것을 확인했다.

## 회귀와 런타임 확인

전체 Vitest 회귀는 **16개 테스트 파일, 43개 테스트**를 통과했고, 일회성 TypeScript 검사와 프로덕션 빌드도 통과했다. 빌드 시 정적 저장소 경로와 런타임 SDK 지연 로드에 관한 기존 경고는 남지만 빌드 실패는 아니다. 최신 서버 소스는 `server/publicApiCors.ts`만 참조하며 삭제된 `server/piperCors` import를 참조하지 않는다. 개발 로그의 `piperCors` 오류는 삭제 순간인 2026-08-27 09:38:55에 기록된 과거 항목으로, 이후 이번 검증 중 재발하지 않았다.

최종 브라우저 확인에서 히어로의 안내 문구와 일일 학습지 배경에 사용되는 사진 위에 진청색 배경이 적용된 것을 확인했다. 마지막 우선순위 보완은 기존 포스터 스타일을 유지하되, 밝은 장면에서도 흰색 본문이 배경 사진만으로 대비를 확보하는 방식에 의존하지 않도록 한다.

실제 렌더링된 홈에서 WCAG 2.0·2.1·2.2 A/AA 태그를 대상으로 실행한 브라우저 기반 axe 감사는 `meta-viewport` 확대 제한과 작은 답안 확인 버튼을 보완한 뒤 **위반 0건**을 반환했다. 다만 배경 이미지와 반투명 레이어의 조합은 자동 계산할 수 없어 `color-contrast` 항목이 136건의 검토 필요 상태로 남았다. 따라서 아래의 보수적 합성 대비 비율 계산과 리포트 화면의 별도 검증을 계속 수행한다.

| 검토 대상 | 전경·최악 조건 합성 배경 | 보수적 대비 비율 | AA 일반 텍스트 기준 |
| --- | --- | ---: | --- |
| 헤더 흰색 텍스트 | `#FFFFFF` / `rgba(#033653, .88)`을 흰색 배경에 합성 | 8.92:1 | 통과 |
| 히어로 흰색 텍스트 | `#FFFFFF` / `rgba(#03354F, .86)`을 흰색 배경에 합성 | 8.54:1 | 통과 |
| 일일 학습 패널 흰색 텍스트 | `#FFFFFF` / `rgba(#033955, .84)`을 흰색 배경에 합성 | 7.59:1 | 통과 |
| 답안 흰색 텍스트 | `#FFFFFF` / `rgba(#03354F, .90)`을 흰색 배경에 합성 | 9.66:1 | 통과 |
| 리포트 카드 흰색 텍스트 | `#FFFFFF` / `rgba(#033650, .88)`을 흰색 배경에 합성 | 8.97:1 | 통과 |
| 노란 주요 버튼의 진청 텍스트 | `#143F55` / `#FFE276` | 8.76:1 | 통과 |
| 선택된 주간 탭의 먹색 텍스트 | `#20201E` / `#F3D547` | 11.21:1 | 통과 |

보호자 리포트는 이미지 위 헤더와 도입부에도 진청색 대비 배경을 적용했고, 실제 브라우저 측정에서 해당 배경값과 카드 배경값이 반영된 것을 확인했다. 리포트 대상 WCAG 2.0·2.1·2.2 A/AA axe 감사도 **위반 0건**을 반환했다. 다만 이미지·투명 레이어의 자동 색 대비 계산 한계로 `color-contrast`는 70건의 검토 필요 상태이며, 위 표의 최악 조건 합성 계산으로 핵심 텍스트·제어 조합을 별도로 확인했다. 모든 학습 진행률은 이름·최솟값·최댓값·현재값을 가진 `progressbar`로 노출된다.

최종 회귀에서는 **17개 테스트 파일, 45개 테스트**와 일회성 TypeScript 검사·프로덕션 빌드가 모두 통과했다.

추가 검증에서 기존 포스터 스타일의 동등 특이도 규칙이 일부 반투명 배경을 다시 적용하는 것을 확인해, 접근성 스타일의 선택자 우선순위를 보완했다. 최종 홈 계산 스타일은 히어로 `#03354F`, 일일 소개·학습 패널 `#033955`, TTS `#072F46`, DotPad 패널 `#0B4669`, 답안 `#03354F`, 헤더 `#033653`의 불투명 배경과 흰색 전경으로 확인되었다. 이 조합은 배경 이미지의 밝기에 의존하지 않는다.

보호자 리포트도 최종 측정에서 헤더 `#033653`, 도입부 `#03354F`, 요약·상세 카드 `#033650`, 추천 카드 `#022B43`의 불투명 배경으로 확인되었다. 리포트 현재 요일·영역별 완료 수에서 발견된 저대비 항목은 현재 요일을 `#FFF0B3` 배경과 진청 글자로, 완료 수를 흰색 글자로 조정해 브라우저 `color-contrast` 규칙의 **위반 0건**으로 해소했다. 자동 도구가 배경 이미지·가상 요소의 색을 확정하지 못한 4건은 검토 필요 상태로 남지만, 핵심 텍스트와 제어는 불투명 배경·실제 계산 색상·비율 표로 교차 검증했다.

홈의 최종 `color-contrast` 규칙도 **위반 0건**을 반환했다. 그러나 포스터형 페이지는 글자 자체의 투명 배경과 부모의 불투명 패널을 조합하는 구조여서, 자동 도구가 이 상속 관계를 확정하지 못한 109개 항목을 검토 필요으로 표시한다. 이 항목에는 히어로 제목·본문·버튼, TTS·탭·섹션 제목 등이 포함되며, 최종 계산 스타일은 각 상위 패널의 불투명 배경과 흰색 또는 고대비 전경이 적용된 것을 확인했다. 따라서 자동 도구의 **위반 0건**과 실제 계산 색상·아래 직접 대비 계산을 함께 근거로 삼고, 보조기기 실사용 검증은 별도 제한으로 유지한다.

| 최종 핵심 조합 | 전경 / 불투명 배경 | 직접 대비 비율 | AA 일반 텍스트 기준 |
| --- | --- | ---: | --- |
| 홈 헤더 | `#FFFFFF` / `#033653` | 12.68:1 | 통과 |
| 히어로 | `#FFFFFF` / `#03354F` | 12.92:1 | 통과 |
| 일일 학습 | `#FFFFFF` / `#033955` | 12.21:1 | 통과 |
| TTS·DotPad | `#FFFFFF` / `#072F46`, `#0B4669` | 13.96:1, 10.03:1 | 통과 |
| 리포트·추천 카드 | `#FFFFFF` / `#033650`, `#022B43` | 12.75:1, 14.69:1 | 통과 |
| 주요 노란 버튼 | `#143F55` / `#FFE276` | 8.76:1 | 통과 |
| 선택된 주간 탭 | `#20201E` / `#F3D547` | 11.21:1 | 통과 |
| 현재 리포트 요일 | `#033650` / `#FFF0B3` | 11.15:1 | 통과 |
