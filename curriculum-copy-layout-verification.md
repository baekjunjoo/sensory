# 학습 정원 카드 문구 정렬 검증

학습 정원 카드는 요일, 제목, 서브문구를 서로 떨어진 개별 요소가 아니라 `curriculum-copy` 한 묶음으로 구성했다. 문구 묶음은 카드의 하단 읽기 축에 고정하고, 캐릭터와 3D 배경은 뒤쪽 장면으로 유지했다. 별도의 불투명 텍스트 박스는 추가하지 않았다.

| 화면 | 확인 결과 |
|---|---|
| 모바일 390×844 | 카드가 한 열로 정리되어 충분한 문단 폭을 확보했다. `THU → 영어 → 소리와 철자를 함께 점자로 연결해요.` 및 `FRI · SAT → 촉각 그림 → 도형과 지도 속 정보를 손끝으로 읽어요.`가 한 흐름으로 이어지고, 단어 단위의 과도한 줄바꿈이 발생하지 않았다. |
| 데스크톱 1280×720 | 네 카드는 동일한 하단 축에서 요일·제목·설명을 배치한다. 서로 다른 캐릭터 장면과 겹치지 않고, 카드 사이의 정렬 리듬이 유지된다. |
| 회귀 | 홈 테스트에 네 카드가 요일·제목·설명을 가진 `curriculum-copy` 묶음으로 렌더링되는 검증을 추가했다. |

> 글자와 캐릭터의 안전 영역은 유지하되, 콘텐츠가 세로로 붕괴되지 않도록 작은 화면에서는 2열 카드 대신 한 열 카드로 전환한다.
