# 점자 실험실·DotPad 문의 영역 검증 기록

## 구현 범위

점자 실험실 제목의 **점점점**은 밝은 크림색 글자와 주황 밑줄로 변경해 진청 배경에서 바로 읽히게 했다. 기존 `.studio-face`와 마지막 영역의 `.footer-plant`는 CSS로만 숨기지 않고 마크업에서도 제거했다. 입력창과 점자 프리뷰 사이에는 `24px`(모바일 `20px`) 간격을 만들고, 프리뷰 하단 상태 행에는 점선 구분선과 상단 여백을 추가했다.

최하단에는 Sensory가 직접 판매·렌탈을 운영한다고 오인되지 않도록, **Dot Inc. 공식 문의 페이지**와 공식 제품 페이지로 연결되는 렌탈·구매 문의 영역을 배치했다. 근거와 링크는 [DotPad 문의 연결 출처](./dotpad-inquiry-source.md)에 별도로 기록했다.

## 개발 화면 확인

| 항목 | 데스크톱 1280px | 모바일 390px |
|---|---|---|
| 점점점 강조 | 크림색 글자·주황 밑줄이 진청 배경에서 선명하게 표시 | 제목 폭 안에서 세 줄 흐름을 유지하며 강조가 식별됨 |
| DotPad 프리뷰 | 입력창·점자 프리뷰·상태 행이 별도 간격과 점선으로 구분 | 같은 순서로 1열 배치되며 가로 넘침 없음 |
| 장식 캐릭터 | 점자 실험실과 마지막 ‘내일’ 영역 모두 캐릭터 없음 | 동일하게 제거됨 |
| 문의 영역 | 하단 아이보리·민트 카드에 공식 문의 CTA와 제품 링크 표시 | CTA와 제품 링크가 1열로 전환되어 손가락 조작 가능 |

`Home.friendProfile.test.tsx`에 점자 실험실·푸터 장식 캐릭터 부재와 두 공식 링크를 확인하는 회귀 검사를 추가했다. 전체 `23개` 테스트 파일·`72개` 테스트, TypeScript 검사와 Vite 프로덕션 빌드를 통과했다.

## GitHub Pages 공개 확인

최신 GitHub Pages 배포에서 점자 실험실의 `점점점` 강조와 `LIVE DOTPAD PREVIEW` 입력·촉각 프리뷰·상태 행이 표시되는 것을 확인했다. 점자 실험실에는 하단 장식 캐릭터가 없으며, 마지막 `내일도 새로운 한 장을 만나.` 영역에도 캐릭터가 남아 있지 않았다.

공개 페이지 최하단에는 아이보리·민트 카드 형태의 **DotPad 렌탈·구매 문의하기** CTA와 `Dot Pad X 자세히 보기` 보조 링크가 보인다. 두 링크는 각각 [Dot Inc. 공식 문의 페이지](https://www.dotincorp.com/en/contact)와 [Dot Pad X 공식 제품 페이지](https://www.dotincorp.com/en/product/dotpadx)를 새 창으로 연다. CTA와 보조 링크 모두 접근 가능한 링크로 노출되며, 캐릭터 장식 없이 문의 목적을 선명하게 전달한다.

개발 화면의 `1280×720px`·`390×844px` 전체 검토에서 점자 실험실의 주황 밑줄 강조, 입력·DotPad 프리뷰의 분리 간격, 장식 캐릭터의 부재, 최하단 문의 카드의 1열 전환을 확인했다. 하단 마감 제목은 데스크톱에서 내용 폭을 제한하지 않고, 모바일에서만 `29–34px`로 축소해 원래의 두 줄 구조가 불필요하게 세 줄 이상으로 꺾이지 않도록 보정했다.

최종 화면에서는 ‘내일’ 안내가 장식 카드가 아닌 푸터 상단의 간결한 마감 행으로, 그 아래 DotPad 문의 카드와 브랜드 푸터가 순서대로 이어진다. 데스크톱에서는 안내 문구와 보호자 리포트 링크가 같은 행에 정렬되고, 모바일에서는 안내·링크·문의 카드가 충분한 세로 간격으로 한 열에 배치된다.
