# 학습·리포트 검증 기록

보호자 리포트는 7일 중 월요일과 화요일의 샘플 완료 기록을 초기값으로 표시하며, 수요일의 `촉각 덧셈`을 다음 학습지로 안내한다. 학습지 화면에는 월요일부터 일요일까지의 7개 선택 항목과 캐릭터별 안내 버튼이 표시되며, 데스크톱 레이아웃에서 학습지·캐릭터·리포트 링크의 겹침은 관찰되지 않았다.

수요일 `촉각 덧셈`의 정답 `5`를 선택해 완료하면 브라우저 저장 기록에 `d3`가 추가된다. 이어서 보호자 리포트를 열었을 때 완료 수는 3/7, 집중 시간은 30분, 진행률은 43%, 수학 영역은 1/1 완료로 갱신되며, 최근 성취에 루루의 정답 피드백이 표시된다.

Googly eyes 업데이트 후 홈 화면의 34개 동공 요소를 대상으로 포인터 이벤트를 검증했다. 데스크톱 포인터 이동과 터치 포인터 누름 모두에서 동공의 변환값이 갱신됐으며, 동공 위치는 각 흰자 내부의 제한된 범위 안에 유지된다.

단색 캐릭터 업데이트 후 루루를 선택하면 문서 테마가 `lulu`로 전환되고 전역 주색상이 핑크 계열로 갱신된다. 3.2초의 유휴 상태 뒤에는 깜빡임 상태가 활성화되고, 포인터를 움직이면 즉시 해제된다. 목요일 영어 점자 문제의 정답 `C`를 완료하면 캐릭터 점프 상태와 완료 피드백이 함께 표시된다.

동공 추적은 포인터 이벤트에서 즉시 좌표를 적용하지 않고, 프레임 보간으로 목표 좌표에 접근하도록 개선했다. 테스트에서 연속 프레임의 이동값이 단계적으로 증가했고, 약 220ms 뒤 안정 위치에 도달해 끊김 없는 감속 동작을 확인했다.

캐릭터 프로필 확장 후 화면에는 모모·피오·루루·나비의 식별자가 모두 적용됐고, 캐릭터별 호흡 애니메이션이 활성화됐다. 목요일 영어 점자 학습지에서 정답 C를 완료하면 나비 완료 메시지가 표시되며, 정답 반짝임·점프 상태가 학습지 캐릭터에 연결된다.

3D 토이 섬 기반 학습지에서는 요일 탭을 고르면 기존 카드가 잠시 퇴장한 뒤 새 학습지로 교체되고, 파스텔 파도색과 3D 섬 이미지를 사용한 오버레이가 그 사이를 연결한다. 모션 감소 환경에서는 이 전환 상태를 건너뛰고 새 학습지를 즉시 표시하도록 구성했다.

브라우저에서 수요일 `촉각 덧셈`에서 목요일 `첫 영어 점자`를 선택했을 때, 80ms 시점에는 `exit` 상태와 기존 학습지가 유지됐고, 400ms 시점에는 `enter` 상태와 새 학습지가 표시됐다. 이후 전환이 완료되면 상태는 `idle`로 복귀했다.

월요일부터 일요일까지 7개 탭을 순서대로 확인한 결과, 히어로와 학습지의 3D 배경은 각각 조개 글자 해변, 촉각 우편 만, 숫자 산호 공방, 알파벳 구름 정원, 입체 지도 모래섬, 도형 퍼즐 만, 별빛 이야기 해안의 서로 다른 정적 자산으로 교체됐다.

요일 탭 전환은 이제 기존 배경을 별도 레이어로 잠시 유지한 뒤 720ms 동안 새 3D 배경 위에서 사라지도록 구성됐다. 따라서 새 학습지 내용은 즉시 준비하면서도 화면상 배경은 끊기지 않는 크로스페이드로 연결된다. 모션 감소 설정에서는 이전 레이어와 애니메이션을 생략하고 즉시 전환한다.

오늘의 학습을 완료하면 해당 히어로에 해질녘의 따뜻한 색조와 차분한 남색 음영이 순차적으로 더해지고, 0.45초 뒤 작은 별빛 네 개가 나타난다. 완료 상태는 장면 레이블의 `SUNSET COMPLETE` 텍스트와 색상 대비로도 중복 전달하며, 완료 전 화면은 데스크톱 1280×720과 모바일 390×844에서 확인했다.

풀블리드 3D 포스터 UI 적용 후 홈의 히어로는 화면 폭과 높이를 3D 섬 장면으로 채우고, 제목·설명·CTA·친구 선택을 별도 종이 카드 대신 배경 위에 직접 배치한다. 데스크톱 전체 페이지와 모바일 390×844에서 큰 제목, 행동 버튼, 하단 탐색, 학습지 오버레이의 대비와 터치 영역을 확인했다.

보호자 리포트도 같은 3D 섬 배경 위에 큰 주간 제목과 반투명 지표 패널을 올리는 방식으로 통일했다. 모바일에서는 제목을 단어 단위로 줄바꿈해 읽기 흐름을 유지하고, 데스크톱에서는 비어 보이던 우측 장식 영역을 제거하여 배경 장면과 주간 요약이 하나의 포스터처럼 이어지는 것을 확인했다.

성장 카드의 여러 보조 캐릭터·도트 장식을 하나의 큰 나비 캐릭터로 정리했고, 커리큘럼 네 카드도 각각 모모·피오·루루·나비 하나만 하단 우측에 크게 배치했다. 카드의 제목·설명은 좌상단 안전 영역을 유지하며, 데스크톱 전체 페이지와 모바일 390×844에서 캐릭터와 텍스트·카드 경계가 겹치지 않는 것을 확인했다.

캐릭터 확대 보정 후 성장 카드의 나비 캐릭터는 카드 하단의 넓은 면적을 차지하고, 커리큘럼 카드는 두 열의 넓은 구성 안에서 캐릭터가 하단 절반을 차지하도록 조정됐다. 정밀 포인터 환경에서 카드에 마우스를 올리면 캐릭터만 1.8초 주기의 짧은 부유·기울기·확대 모션과 밝기 변화로 반응하며, `prefers-reduced-motion` 환경에서는 해당 모션이 정지한다. 데스크톱과 모바일 전체 페이지에서 크기와 텍스트 분리를 확인했고 타입 검사와 프로덕션 빌드도 통과했다.

모모는 점자 타일, 피오는 수 세기 블록, 루루는 그림책, 나비는 촉각 지도를 든 단독 3D 토이 자산으로 각각 생성해 문해·수학·영어·촉각 그림 카드에 직접 연결했다. 기존의 다중 캐릭터 장면 크롭을 제거하고 투명 배경의 전신 캐릭터만 남겼다. 데스크톱 1280×720과 모바일 390×844에서 네 자산이 한 카드에 한 명씩 표시되고 제목·설명과 분리되는 것을 확인했다.

음성 안내는 기기에서 제공되는 한국어 음성 중 Google·Microsoft·Natural 계열을 우선 선택하도록 개선했다. 모모·피오·루루·나비는 서로 다른 속도·톤·볼륨 프로필로 발화하며, 문장부호 뒤의 짧은 휴지와 이전 발화 취소 토큰으로 연속 클릭 시 겹침을 줄였다. 음성을 끄면 진행 중 발화를 즉시 중단하고, 동일한 내용은 라이브 영역으로 유지한다. 타입 검사와 프로덕션 빌드를 통과했고 홈 화면을 확인했다.

Piper 기반 다국어 접근성 음성 읽기를 전체 스택 서버에 추가했다. 한국어 `ko_KR-kss-medium`과 영어 `en_US-lessac-low` 모델을 사용자 정의 배포 이미지에서 내려받고, Node 서버는 Piper HTTP 하위 프로세스를 한 번만 시작하도록 구성했다. 텍스트·언어·속도 키의 LRU 메모리 캐시 및 동일 요청 공유를 적용했다.

개발 서버에서 한국어 합성 API가 WAV base64 데이터를 반환하는 것을 확인했고, 동일 문장을 연속 요청한 결과 첫 합성은 0.185초, 메모리 캐시 응답은 0.015초였다. 페이지에는 읽을 영역·언어·속도·재생·일시정지·정지 컨트롤과 라이브 상태 안내가 표시되며, Piper 연결 실패 시 브라우저 음성으로 대체하도록 구성했다. 타입 검사, 단위 테스트 2건, 프로덕션 빌드, 데스크톱 전체 화면을 확인했다.

접근성 패널에는 실제 화면 선택 텍스트를 최대 560자까지 읽는 제어를 추가했다. 포커스가 입력·버튼·링크가 아닐 때 Space는 재생·일시정지를, Escape는 정지를 수행한다. 합성 실패는 서버 단위 테스트에서 전파되는 것을 확인했고, 클라이언트는 브라우저 음성 지원 여부를 점검한 뒤 대체 읽기 또는 명시적 텍스트 안내를 제공한다. Vitest 범위를 클라이언트 공통 모듈까지 확장해 브라우저 음성 미지원·언어·속도 전달·읽기 완료 상태까지 검증했으며, 타입 검사와 총 5건의 단위 테스트를 통과했다.

Piper WAV 재생 오류를 `player.onerror`에서 브라우저 음성 대체 함수로 연결하는 경로를 공통 함수로 분리했고, 오류 발생 시 대체 콜백이 정확히 한 번 실행되는 것을 단위 테스트로 확인했다. 최종 검증에서 서버 캐시·합성 실패·브라우저 음성 미지원·언어/속도 전달·읽기 완료·Piper 오디오 오류 대체를 포함한 총 6건의 테스트와 타입 검사·프로덕션 빌드를 통과했다.

Piper 오디오 오류에서 브라우저 음성 대체 상태까지 이어지는 흐름을 공통 함수로 확장해, 재생 오류 뒤에는 `Piper 연결을 기다리는 동안 브라우저 음성으로 읽고 있어요.` 상태가 노출되고 브라우저 음성이 없는 환경에서는 텍스트만 읽도록 안내하는 것을 테스트했다. 최종적으로 3개 테스트 파일의 8개 테스트, 타입 검사, 프로덕션 빌드가 통과했다.

`AccessibilityTts`를 jsdom 렌더링 테스트로 검증해 실제 Piper 오디오 오류 뒤 브라우저 음성 안내가 라이브 상태 영역에 나타나는 것을 확인했다. `speechSynthesis`가 없는 환경에서는 `자연 음성과 브라우저 음성을 사용할 수 없어요. 화면의 텍스트를 계속 읽어 주세요.`라는 텍스트 전용 안내가 표시된다. 최종적으로 4개 테스트 파일의 10개 테스트, 타입 검사, 프로덕션 빌드를 통과했다.

영어 `en-US` 합성 요청도 실제 개발 서버 API에서 WAV base64 데이터를 반환했고, 응답 크기는 111,400바이트였다. 접근성 TTS 컴포넌트 테스트는 라이브 상태 영역, 읽기·정지 버튼의 이름과 `Space`·`Escape` 단축키, 실제 단축키에 따른 재생·정지 상태를 확인했다. 전역 키보드 이벤트의 대상이 Window인 경우까지 안전하게 처리하도록 보완했으며 최종적으로 4개 테스트 파일의 11개 테스트, 타입 검사, 프로덕션 빌드를 통과했다.

Piper 기본 재생 성공 경로를 접근성 TTS 렌더링 테스트에 추가했다. 읽기 클릭 후 Piper 오디오의 `play()`가 실행되고 브라우저 `speechSynthesis.speak()`는 호출되지 않으며, Piper 재생 오류에서만 브라우저 음성 대체가 한 번 실행되는 것을 확인했다. 최종적으로 4개 테스트 파일의 12개 테스트, 타입 검사, 프로덕션 빌드를 통과했다.

GitHub Pages의 이전 화면은 설정이 `main`이 아닌 `gh-pages` 브랜치의 루트를 제공하고 있어 계속 표시되는 것으로 확인했다. GitHub Pages는 Node·Python 서버를 실행할 수 없으므로 Piper를 기본 음성으로 제공할 수 없다. React·Piper 프로젝트는 Manus의 서버 호스팅에서 실행되어야 하고, GitHub 저장소는 최신 소스 관리 용도로 반영된다.

Piper에는 한국어·영어에 더해 스페인어(`es-ES`) 모델을 추가했고, 스페인어 실합성 API가 WAV base64 응답을 반환하는 것을 확인했다. 페이지 문단은 문장별로 순차 재생되며 현재 문장은 `▶` 표식과 고대비 배경으로 하이라이트된다. 사용자는 재생 속도(0.85×–1.3×)와 피치(낮게·기본·높게)를 직접 바꿀 수 있고, 제어값은 클라이언트 재생에 적용해 같은 문장·언어의 Piper 음성을 캐시에서 재사용한다. 데스크톱과 390px 모바일 화면, 15개 테스트, 타입 검사, 프로덕션 빌드를 확인했다.

사용자 승인에 따라 기존 GitHub Pages 정적 초안을 교체하고 `baekjunjoo/sensory`의 `main`을 커밋 `ac0f79b3007909228b63ecd1b11cfdee33aaa726`으로 강제 반영했다. 원격 저장소에서 `server/piperTts.ts`, `client/src/components/AccessibilityTts.tsx`, `Dockerfile`을 확인했다. 최종 재검증에서도 Piper API는 한국어 요청에 `audioBase64`를 반환했고, 동일 문장은 첫 요청 0.156초·캐시 요청 0.004초로 응답했다.

GitHub Pages의 이전 화면은 GitHub Pages 설정이 `main`이 아니라 별도 `gh-pages` 브랜치의 루트를 제공하고 있기 때문에 계속 표시되는 것으로 확인했다. 해당 정적 호스팅은 Node·Python 서버를 실행할 수 없으므로 Piper를 기본 음성으로 제공할 수 없다. 현재 React·Piper 프로젝트는 Manus의 서버 호스팅에서 실행되어야 하며, GitHub 저장소는 소스 관리 용도로 최신화돼 있다.

접근성 TTS는 한국어·영어에 스페인어(`es-ES`) Piper 모델을 추가했고, 스페인어 실합성 API도 WAV base64 응답을 반환했다. 텍스트를 문장으로 나누어 한 문장씩 합성·재생하며, 현재 문장은 `▶` 표시와 고대비 배경으로 시각적으로 하이라이트된다. 사용자는 재생 속도(0.85×–1.3×)와 피치(낮게·기본·높게)를 직접 바꿀 수 있고, Piper 캐시는 발화 텍스트·언어 중심으로 유지해 제어값 변경 시 동일 음성을 재사용한다. 데스크톱과 390px 모바일 화면에서 컨트롤·하이라이트의 배치를 확인했다.

다국어 Piper·문장 하이라이트·속도·피치 변경은 `baekjunjoo/sensory`의 `main`에 커밋 `11ce226a9b54c84d6c6d9ba0ac5eb2281891da9c`으로 반영했다. GitHub Pages는 `gh-pages`의 정적 파일만 제공하므로 이전 화면을 유지하는 것이 정상이며, Node·Python Piper 서버를 실행할 수 없다. 실제 자연 음성 기능은 프로젝트의 Manus 서버 배포 환경에서 동작하도록 준비됐고, 배포는 사용자가 관리 화면의 Publish 버튼으로 실행해야 한다.

GitHub Pages와 Piper 서버를 분리하는 구조를 추가했다. `main`의 GitHub Actions 워크플로는 `/sensory/` 경로의 정적 프런트엔드를 만들며, 저장소 변수 `PIPER_API_URL`에 Manus 배포 주소가 있으면 해당 서버의 tRPC 음성 API를 호출한다. 주소가 비어 있거나 연결에 실패하면 브라우저 음성 대체를 유지한다. Piper API는 `https://baekjunjoo.github.io` Origin에만 CORS 응답을 제공하며, 실제 OPTIONS preflight에서 POST·OPTIONS·Content-Type·Authorization 헤더를 확인했다. 19개 테스트와 GitHub Pages 전용 빌드를 통과했다.

GitHub Pages는 공개 GitHub 릴리스의 최적화된 3D WebP 장면·캐릭터 자산을 직접 읽도록 바꿔 최신 화면이 정상 표시되는 것을 확인했다. 현재 Autoscale 호스팅에서는 Piper 서버가 유휴 후 깨어나는 구간에 외부 `github.io` 요청이 일시적으로 실패할 수 있다. 사용자가 Reserved Hosting 전환을 선택하지 않은 상태에서는 이 경우 브라우저 음성을 접근성 대체로 유지하며, `Piper 연결을 기다리는 동안 브라우저 음성으로 읽고 있어요.` 상태를 노출한다. 대체 중에도 문장 하이라이트·일시정지·정지·언어·속도·피치 제어는 계속 동작한다.

GitHub Actions Pages 워크플로는 커밋 `6dd82552b51518970580df13a946a379491f9242`에서 성공적으로 완료됐고, `https://baekjunjoo.github.io/sensory/?v=6dd8255`에서 최신 3D 섬 배경·카드 캐릭터·접근성 패널이 표시되는 것을 확인했다. 3D 자산은 `https://github.com/baekjunjoo/sensory/releases/tag/sensory-assets-v1` 릴리스의 WebP 파일을 사용한다. GitHub Pages의 읽기 요청은 `https://sensorytac-v8ekttiz.manus.space/api/trpc/accessibilityTts.synthesize?batch=1`로 전달되며, 해당 Autoscale API가 연결되지 않은 경우 브라우저 음성 대체 상태를 표시한다.

GitHub Pages 런타임의 Performance Resource 목록에서 `https://sensorytac-v8ekttiz.manus.space/api/trpc/accessibilityTts.synthesize?batch=1` 요청을 확인했다. 이는 GitHub Actions 빌드 시 저장소 변수 `PIPER_API_URL` 값이 정적 번들에 실제 주입됐음을 보여 준다. pnpm 설정 단계 추가 뒤 발생한 packageManager 버전 중복은 워크플로의 명시 version 설정을 제거해 해소했고, 최신 GitHub Actions Pages 실행은 성공했다.

동일한 GitHub Pages 환경 변수로 로컬 정적 빌드를 다시 만들고 `dist/public`을 검사한 결과, `https://sensorytac-v8ekttiz.manus.space`가 번들에 직접 포함된 것을 확인했다. 따라서 `PIPER_API_URL`은 정적 산출물과 GitHub Pages 런타임 양쪽에서 모두 주입됐다. 현재 외부 자연 음성 실패 원인은 변수 누락이 아니라 Autoscale Piper 서버의 외부 연결이 간헐적으로 종료되는 현상이며, 이 경우에는 세 번의 재시도 후 브라우저 음성 대체가 작동한다.

Autoscale 유지 선택에 맞춰 Piper 첫 요청 실패 시 0.8초·1.2초·1.8초 간격으로 세 차례 재시도한 뒤에만 브라우저 음성으로 대체하도록 보완했다. 정지 버튼을 누르면 보류 중인 재시도 타이머가 취소되며, 재시도·취소·대체 중복 방지까지 포함한 22개 테스트, 타입 검사, GitHub Pages 빌드를 통과했다. 최신 GitHub Pages에서 읽기를 실제로 실행한 결과, 3D 배경·캐릭터·문장 하이라이트는 정상 표시됐고 현재 Autoscale Piper 연결은 재시도 후 브라우저 대체로 전환됐다.

접근성 TTS의 언어·속도·피치 설정을 `sensory-accessibility-tts-preferences` 로컬 저장소 키로 저장하고 다음 컴포넌트 마운트에서 지원되는 값만 복원하도록 구현했다. 읽는 문장은 `scrollIntoView`로 가장 가까운 가시 영역에 부드럽게 이동하며, 모션 감소 설정에서는 즉시 이동한다. 저장 값 복원·유효하지 않은 값 대체·저장 실패 무시·활성 문장 스크롤·모션 감소 스크롤을 포함해 29개 테스트와 타입 검사·프로덕션 빌드를 통과했다. 모바일 390×844 화면에서도 기존 큰 타이포그래피·CTA·하단 탐색이 겹치지 않는 것을 확인했다.

재시도 후 Piper가 성공한 경우의 컴포넌트 경로도 추가 검증했다. 첫 합성 오류 뒤 0.8초 재시도에서 Piper 오디오가 재생되고 브라우저 `speechSynthesis`는 호출되지 않으며, 실패·정지 취소·성공 모두 중복 재생 없이 처리된다. 최종적으로 7개 테스트 파일의 23개 테스트와 타입 검사를 통과했다.
