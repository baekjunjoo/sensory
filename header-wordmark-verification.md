# 헤더 워드마크 검증 기록

2026-08-27 개발 미리보기의 데스크톱 1280×720 화면에서 헤더 왼쪽에 **`sensory` 텍스트만** 표시되는 것을 확인했습니다. 기존의 점자 그래픽과 `TOUCH & GROW` 태그라인은 헤더에서 제거됐으며, 주 메뉴 및 시작하기 버튼의 정렬은 유지됩니다.

모바일 390×844 화면에서도 `sensory` 워드마크와 메뉴 버튼이 한 줄에 표시되며, 가로 넘침 또는 겹침은 관찰되지 않았습니다.

개발 화면에서 헤더의 `오늘의 한 장` 메뉴를 실제로 선택했을 때 URL이 `#today`로 유지되고 학습지 섹션까지 이동하는 것을 확인했습니다. 헤더 브랜딩 마크업만 감추었으며, 기존 메뉴 링크와 클릭 처리는 변경하지 않았습니다.

모바일 메뉴 버튼과 모바일 탐색 영역은 개발 DOM에서 확인했다. 연속 클릭을 한 검사에서는 React 상태 갱신이 하나의 검사 흐름에 겹치므로, 다음 검사는 새로 고친 화면에서 한 번의 열기 동작과 갱신된 상태를 분리해 확인한다.

새로 고친 초기 화면에서 모바일 메뉴 버튼을 한 번 선택한 뒤 120ms를 기다려 확인했다. 탐색 영역의 `hidden` 값은 `true`에서 `false`로, 버튼의 `aria-expanded` 값은 `false`에서 `true`로 갱신되어 실제 열기 동작이 유지됨을 확인했다.
