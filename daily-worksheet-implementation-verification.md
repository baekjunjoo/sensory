# 매일 학습지 경험 확장 구현 검증

## 구현 범위

이번 구현은 기존 7일 학습지와 캐릭터·Liblouis·Super Dot TTS·DotPad 연결 경험을 유지하면서, 학습의 시작과 끝을 한 장의 일상 루틴으로 묶는다. 각 학습지는 아직 열지 않았을 때 **오늘의 봉투 열기** 화면을 먼저 표시한다. 봉투를 열면 3단계 촉각 미션으로 진입하며, 정답을 맞히면 **완료 도장**, 연속 학습 수치, 촉각 스티커, 보관함, 다음 학습지 미리 보기로 이어진다.

| 구간 | 구현한 동작 | 저장 상태 |
|---|---|---|
| 1. 도착 | 요일·영역·예상 시간을 담은 봉투 열기와 학습지 열기 | `openedIds` |
| 2. 탐색 | DotPad 프레임 전송 또는 화면 점자 대체 경로 중 선택 | `tactileExploredIds`, `dotpadFrameIds` |
| 3. 발견 | 답 선택·정답 확인·완료 도장·다음 날 미리 보기 | `completedIds`, `completedAt` |
| 되돌아보기 | 보관함에서 영역·완료·다시 만나기 필터 및 학습지 재진입 | `reviewIds` |

## 보관함과 보호자 리포트

보관함은 7일 학습지를 날짜, 감각 영역, `완료`, `열어 봄`, `도착 대기`, `다시 만나기` 상태로 분류한다. 각 카드의 **학습지 열기**는 해당 요일을 선택한 뒤 홈의 학습지 영역으로 돌아간다. GitHub Pages는 SPA 직접 경로 대신 `?view=archive`를 사용하며, 앱 환경에서는 `/archive`를 사용한다.

보호자 리포트는 기존 주간 진도와 영역별 현황을 유지하면서 봉투를 연 횟수, 화면 또는 DotPad로 점을 탐색한 횟수, 다시 만나기 목록 수, DotPad 프레임 전송 맥락을 추가했다. `DotPad에 보냈다`는 앱이 전송 요청을 성공적으로 시작한 기록이며, 실기기에서 점이 실제로 표시됐다는 검증을 의미하지 않는다.

## 검증 결과

| 항목 | 결과 |
|---|---|
| 루틴 상태 전환 | 봉투 열기, 완료 시간, 화면 점자 대체, DotPad 프레임 기록, 다시 만나기, 연속 학습 계산 단위 테스트 통과 |
| 사용자 흐름 | 봉투 → 정답 → 완료 도장 → 보관함, 완료 도장 → 다음 날 봉투 전환 회귀 테스트 통과 |
| 보관함 | 완료·다시 만나기 필터, 학습지 재진입 링크, 자동 ARIA 검사 통과 |
| 접근성 | 홈·보관함·리포트의 기존 자동 ARIA 검사와 키보드 탭 패턴 회귀 통과; 색 대비 자동 계산은 이미지 배경의 한계가 있어 기존 수동 검증 범위를 유지 |
| 반응형 | 390px 모바일 및 1280px 데스크톱에서 보관함·리포트의 제목·필터·카드·고정 탐색 배치 확인 |
| 전체 검증 | 20개 테스트 파일, 57개 테스트, 일회성 TypeScript 검사, 프로덕션 빌드 통과 |

> 실제 오디오 출력과 DotPad 320 BLE/USB 연결·촉각 프레임 표시 여부는 사용자 기기, Chrome/Chromium 권한, 연결된 하드웨어 환경에서 별도로 확인해야 한다.

모바일 390px 전체 화면을 재검토해 보관함의 제목·필터·7개 카드와 리포트의 요약·주간 리듬·영역별 현황·다음 학습·일일 리듬·DotPad 맥락·최근 성취·고정 탐색이 세로 흐름에서 겹치지 않는 것을 확인했다. 일일 리듬 카드는 진청색 바탕, 흰색 본문, 연노랑 레이블과 체크로 정리해 모바일에서 제목·레이블·수치가 명확히 읽히도록 보완했다.

GitHub `main`의 커밋 `0d05cadf`로 실행된 Pages 배포 실행 `33127567315`는 성공했다. 공개 홈에서 봉투 도착 화면과 7일 탭을, `?view=archive`에서 보관함·필터·학습지 재진입 링크를, `?view=report`에서 새 일일 리듬·DotPad 맥락을 각각 확인했다.
