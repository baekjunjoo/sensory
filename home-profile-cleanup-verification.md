# 홈 음성 패널 제거와 친구 프로필 중심 정렬 검증

홈 히어로 아래에 있던 **‘기기 음성으로 읽기’** 패널과 전용 조작부를 제거했다. 캐릭터를 눌렀을 때의 짧은 안내 음성과 학습지·DotPad·보호자 리포트 흐름은 그대로 남겨 두었다.

오늘의 친구는 배경 장면의 일부분을 원형으로 잘라 쓰는 방식 대신, 모모·피오·루루·나비 각각의 개별 3D 포트레이트를 `friend-avatar` 원형 프레임 안에 렌더링하도록 변경했다. 캐릭터별로 확대 비율과 세로 위치를 보정해 얼굴이 프레임 중앙에 오도록 했고, 이름은 각 원의 중앙 축 아래에 정렬했다.

| 검증 항목 | 결과 |
|---|---|
| 모바일 390×844 | 네 얼굴이 원형 프레임 안에 중앙 배치되고, 이름·선택 링·고정 하단 탐색과 겹치지 않음 |
| 데스크톱 1280×720 | 네 개의 포트레이트와 이름이 CTA 아래에 균등하게 정렬되고, 배경 장면을 가리지 않음 |
| 회귀 테스트 | 홈 프로필 13개 테스트, 전체 20개 파일·59개 테스트 통과 |
| 코드 품질 | 일회성 TypeScript 검사와 프로덕션 빌드 통과 |

## Bounding box 중심축 측정

실제 Chromium에서 `getBoundingClientRect()`로 측정한 결과, 두 화면 폭 모두 모모·피오·루루·나비의 버튼·원형 프레임·포트레이트 이미지·이름 중심축 편차는 **0px**였다. 선택된 모모의 선택 링은 원형 프레임에 적용되므로 같은 중심축을 사용한다.

| 화면 크기 | 음성 읽기 패널 | 본문 → CTA | CTA → 친구 선택 | 원형 프레임 하단 → 이름 상단 | 친구 프로필 중심축 |
|---|---|---:|---:|---:|---|
| 1280×720 | 없음 | 18px | 8px | 5px | 네 친구 모두 0px 편차 |
| 390×844 | 없음 | 21px | 11px | 5px | 네 친구 모두 0px 편차 |

따라서 홈 음성 패널 제거 뒤에도 본문·CTA·친구 선택은 양수 세로 간격을 유지하며 겹치지 않는다.

> 실제 음성 출력과 DotPad 320의 BLE/USB 물리 기기 전송 검증은 연결된 사용자 환경에서만 수행할 수 있다.
