# 개별 3D 캐릭터 카드 배경 검증 기록

기존 월요일 문해, 수요일 수학, 목요일 영어, 금요일 촉각 그림의 3D 장면을 카드별 배경으로 연결했다. 각 카드는 해당 캐릭터의 투명 개별 3D 자산을 전경으로 유지하며, 왼쪽 텍스트 안전 영역에는 어두운 청록색 오버레이를 적용한다.

현재 개발 화면에서 커리큘럼 카드 영역의 데스크톱·모바일 렌더링과 카드별 배경·전경의 대비를 순차적으로 확인한다.

데스크톱 스크롤 화면에서 기존 3D 캐릭터가 학습지의 전경 요소로 선명하게 표시되고, 반투명 청록 학습 패널과 배경 장면의 대비가 유지되는 것을 확인했다. 다음 화면에서 커리큘럼 카드 네 개의 개별 장면 배경·텍스트 안전 영역을 직접 확인한다.

성장 포스터에서는 나비 개별 3D 자산이 우측 전경에, 입체 지도 모래섬 장면이 카드 전체 배경에 표시됐다. `TOUCH, THEN GROW!` 문구는 좌측의 어두운 오버레이 위에 남아 캐릭터·텍스트·배경이 겹치지 않았다. 커리큘럼 카드 위치로 이동해 네 장면을 계속 확인한다.

커리큘럼 영역의 데스크톱 화면에서 점자 문해 카드에는 조개 글자 해변, 수학 카드에는 숫자 산호 공방이 각각 배경으로 나타났다. 모모와 피오의 개별 3D 캐릭터는 카드 우측 전경에 선명하게 남고, 카드별 청록·보라 오버레이 덕분에 좌측의 과목명·설명·점자가 읽히는 것을 확인했다. 이어 영어·촉각 그림 카드도 동일한 구조와 각자의 장면으로 렌더링된다.

모바일 390×844 전체 화면에서도 네 카드는 한 열로 분리되어 표시됐다. 모모의 조개 글자 해변, 피오의 숫자 산호 공방, 루루의 알파벳 구름 정원, 나비의 입체 지도 모래섬 배경 위에 대응하는 개별 3D 캐릭터가 함께 나타났고, 텍스트는 좌측·하단의 어두운 오버레이 안에 유지됐다. 카드 간 겹침이나 가로 넘침은 관찰되지 않았다. 최종적으로 16개 테스트 파일의 46개 테스트, 타입 검사, 프로덕션 빌드를 통과했다.

데스크톱 정밀 포인터 환경에서는 카드 hover 시 개별 3D 캐릭터의 그림자가 깊어지고 밝기가 소폭 상승하도록 확인했다. 모션 감소 환경에서는 카드·포스터 캐릭터의 애니메이션과 전환을 `none!important`로 차단해 레이아웃을 고정한다. 이 조건은 `curriculumCardScenes.test.ts`에서 직접 회귀 검증했으며, 최종적으로 16개 테스트 파일의 47개 테스트와 타입 검사·프로덕션 빌드를 통과했다.

실제 Chromium 데스크톱에서 점자 문해 카드 위로 포인터를 이동해 측정했다. 카드는 `:hover` 상태였고, 캐릭터 이미지에는 `drop-shadow(0 23px 20px …) brightness(1.04)`가 적용됐다. 카드 배경은 점자 문해 장면의 URL을 포함했고, 캐릭터와 제목 경계는 겹치지 않았다. 이어 Chrome DevTools Protocol로 `prefers-reduced-motion: reduce`를 강제한 뒤 같은 카드에 hover를 재현한 결과 `reduced: true`, `hovered: true`, `animation: none`, `transition: none`, `titleInside: true`, `imageInside: true`를 확인했다.
