# 봉투 도착 화면 재디자인 검증

## 모바일 화면 중간 확인

모바일 390×844 화면을 재검토한 결과, 홈 히어로의 제목·설명·CTA·친구 선택과 고정 하단 탐색은 봉투 화면 스타일 변경 뒤에도 기존 위치와 간격을 유지했다. 봉투 도착 화면의 구체적인 장면 배치와 학습지 열기 흐름은 학습지 섹션 상태에서 별도로 확인한다.

## 학습지 도착 장면 확인

수요일 학습지의 `#today` 위치에서 실제 렌더링을 확인했다. 큰 크림색 독립 카드·두꺼운 테두리·카드 안의 넓은 빈 여백은 제거됐고, 제목·설명·46px 학습지 열기 버튼은 장면 왼쪽에 한 흐름으로 놓였다. 열린 봉투와 `DAY 03 · 수학` 편지는 장면 오른쪽에 작은 소품으로 배치돼, 배경 속 ‘도착한 한 장’으로 읽힌다.

| 화면 | 확인 결과 |
|---|---|
| 모바일 390×844 | 히어로와 하단 탐색은 기존 여백을 유지한다. 학습지 섹션은 한 열 흐름으로 제목·설명·CTA 뒤에 봉투가 이어진다. |
| 데스크톱 | 학습지 문구와 봉투가 좌·우로 분리되며, 봉투는 보조 소품 크기로 유지된다. |
| 학습지 진입 | `학습지 열기` 버튼은 기존 `onOpen` 동작을 유지하며 홈 회귀 테스트로 확인했다. |

데스크톱 1280×720에서는 홈 히어로의 배경 우선 구성과 고정 헤더가 안정적으로 유지되는 것을 확인했다. 수요일 학습지를 선택한 `#today` 화면에서 봉투 도착 영역은 문구·CTA·봉투가 서로 다른 열에 배치되고, 유효한 양의 간격을 유지한다. `학습지 열기`의 기존 상태 전환은 홈 회귀 테스트에서 봉투 → 학습지 → 정답 → 완료 도장 흐름으로 확인했다.

## 전면 3D 봉투 장면 칸

사용자가 제공한 3D 봉투 자산은 점토 질감의 크림색 봉투와 점자 봉인, 바다·모래사장·조개·산호를 함께 포함한 해변 장면이다. 이 장면을 기존의 마스크 처리된 작은 소품이 아니라 `arrival-envelope` 전체 프레임으로 사용했다. CSS는 이미지의 `mask-image`를 해제하고 `object-fit: cover`를 적용해, 봉투와 해변 소품이 한 장면 안에 유지되도록 한다.

개발 화면 390×844 전체 페이지에서 제목·촉각 목표·CTA 뒤에 전체 폭의 해변 3D 봉투 장면이 표시되고 고정 하단 탐색과 겹치지 않는 것을 확인했다. 데스크톱은 문구 열과 장면 열을 분리하고 장면 프레임에 최소 334px 높이를, 모바일에는 260px 높이를 부여한다.

## GitHub Pages 공개 확인

GitHub Pages 최신 배포에서 [수요일 학습지 도착 화면](https://baekjunjoo.github.io/sensory/?lesson=d3#today)을 열어, `TODAY'S DELIVERY 03`, 촉각 목표, 학습지 열기 CTA 다음에 점토 질감 봉투·모래사장·바다 장면이 실제로 표시되는 것을 확인했다. 공개 화면의 문서 구조에도 봉투 자산 `sensory-clay-envelope.png`가 포함되어 있으며, 비스타일 화면이 아닌 기존 3D 섬 배경과 학습지 카드 스타일이 적용된 상태였다.

## 한 장의 편지를 여는 통합 장면

봉투 이미지는 더 이상 별도의 작은 카드나 마스크된 소품이 아니다. `daily-arrival` 전체가 3D 봉투 해변을 배경으로 사용하며, 좌측에는 도착 안내·오늘의 촉각 목표·학습지 열기 CTA가, 우측·하단에는 바다·모래·조개·산호와 함께 점토 봉투가 연결된다. 모바일에서는 같은 장면이 세로형 전체 칸으로 이어져, 한 장의 편지가 도착하고 그 자리에서 학습지를 여는 흐름으로 읽힌다.

사용자가 지적한 캐릭터를 감싸는 투명 프레임은 성장 포스터와 학습 정원 카드의 개별 3D 캐릭터에서 제거했다. 캐릭터 이미지는 배경 위에 직접 놓고 부드러운 그림자만 남겨, 장면의 소품처럼 보이게 했다.

| 검증 화면 | 결과 |
|---|---|
| 모바일 390×844 전체 | 통합 봉투 해변이 촉각 목표·CTA와 하나의 세로 장면을 이루며, 고정 탐색과 겹치지 않음 |
| 데스크톱 1280×720 | 프레임 없는 친구 포트레이트가 히어로 장면 위에 직접 놓이고, 각 이름·선택 상태를 유지함 |
| 회귀 검사 | 통합 장면의 전체 배경·모바일 최소 높이·숨긴 개별 봉투 요소·프레임 제거 규칙을 검사하는 테스트 통과 |

최종적으로 공개 CSS 복구 모의 검사까지 포함한 **23개 테스트 파일·67개 테스트**, TypeScript 검사, 프로덕션 빌드를 통과했다.

## 도착 영역 전체의 편지 장면

사용자 확정 범위에 맞춰 친구별 3D 메인 히어로는 유지하고, `TODAY'S DELIVERY`로 시작하는 오늘의 학습지 도착 영역 전체만 전면 편지 해변으로 변경했다. 이제 봉투 이미지는 별도 카드·프레임·마스크가 아니라 도착 영역 전체를 채우는 3D 점토 해변 배경이다. 점자 봉인이 있는 크림 봉투, 모래·조개·산호는 같은 장면 안에 유지되며, 안내·촉각 목표·학습지 열기 CTA는 어두운 그라데이션 위에서 한 시선 흐름을 이룬다.

모바일 390×844 전체 페이지 검토에서 도착 영역은 다른 섹션과 구분된 작은 이미지 칸 없이 전체 폭의 한 장면으로 이어졌고, 촉각 목표와 CTA 이후에도 봉투·해변 소품이 충분히 보였다. 기존 고정 하단 탐색과는 겹치지 않았다.

## 스크롤 위치별 비겹침 측정

실제 `daily-arrival`로 이동한 Chromium 화면에서 `getBoundingClientRect()`를 측정했다. 데스크톱은 sticky 헤더의 하단 80px 뒤에 도착 영역이 202px부터 시작해 겹치지 않았다. 모바일은 `scroll-margin-top: 82px` 적용으로 도착 영역이 82px부터 시작했고, 제목(152–235px)·촉각 목표(350–370px)·학습지 열기 CTA(389–435px)는 고정 하단 탐색(772–834px)보다 충분히 위에 위치했다.

| 화면 | 도착 영역 | 헤더/도착 겹침 | 제목/목표 겹침 | 목표/CTA 겹침 | 도착 영역/dock 겹침 | 배경 크롭 |
|---|---|---|---|---|---|---|
| 1280×720 | 202–702px | 없음 | 없음 | 없음 | 해당 없음 | `cover`, 최소 500px |
| 390×844 | 82–702px | 없음 | 없음 | 없음 | 없음 | `cover`, 최소 620px |

모바일 도착 영역에는 86px의 하단 스크롤 안전 여백을 두어 고정 탐색이 CTA나 촉각 목표를 가리지 않도록 했다.

## 공개 도착 영역 확인

최신 GitHub Pages의 수요일 학습지에서 주간 탭 아래 `TODAY'S DELIVERY 03` 영역까지 실제로 스크롤했다. 이 영역은 작은 봉투 썸네일이 아니라 점자 봉인·크림 봉투·모래·조개·산호가 있는 3D 해변 배경을 전체 폭으로 사용하며, 도착 안내·촉각 목표·학습지 열기 CTA는 그 위의 좌측 흐름으로 표시된다. 기존 친구별 3D 메인 히어로와 주간 학습지 탭은 이 변경의 대상이 아니며 그대로 유지된다.

## 주간 안내까지 연결한 밝은 장면과 통일 카드

주간 안내·7일 탭을 포함하는 `#today` 전체가 같은 점토 봉투 해변을 배경으로 쓰도록 확장했다. 따라서 주간 제목은 기존의 독립 진청 박스가 아니라 밝은 바다빛·모래빛 배경 위에 놓이고, 7일 탭은 반투명 아이보리 바탕과 선택된 날의 하늘색 상태로 구분된다. 탭의 `role="tab"`, `aria-selected`, 화살표·Home·End 키 이동과 기존 학습지 전환 로직은 변경하지 않았다.

소개 영역은 텍스트와 나비 장면을 같은 최소 높이(데스크톱 338–390px, 모바일 310px)의 짝 카드로 맞췄다. 학습 정원은 데스크톱 2열·모바일 1열의 동일 카드 그리드로 바꾸고, 데스크톱 `4:3` 비율·최소 340px 및 모바일 최소 272px을 적용했다. 카드 문구는 하단 공통 정렬축에서 줄바꿈하며, 3D 캐릭터·장면은 프레임 없이 배경 속에 남는다.

| 검증 항목 | 데스크톱 1280px | 모바일 390px |
|---|---|---|
| 주간 안내와 도착 장면 | 밝은 단일 점토 편지 해변으로 연결, 제목 박스 제거 | 상단 제목·7일 탭·도착 CTA가 한 세로 흐름으로 연결 |
| 소개 짝 카드 | 텍스트·3D 장면이 동등한 높이·모서리·여백 기준 사용 | 두 카드가 동일 폭·최소 높이로 순차 배치 |
| 학습 정원 카드 | 2×2 동일 크기 그리드, 하단 문구 기준선 통일 | 1열 동일 폭·최소 높이 카드로 전환 |
| 회귀 검사 | 23개 테스트 파일·69개 테스트, TypeScript 검사, Vite 빌드 통과 | 동일 빌드 산출물에서 화면 전체 스크린샷 확인 |

`daily-arrival`의 실제 DOM 측정에서도 데스크톱 제목(1540–1653px)·촉각 목표(1753–1773px)·CTA(1792–1838px), 모바일 제목(152–235px)·촉각 목표(350–370px)·CTA(389–435px)의 상호 겹침은 없었다. 모바일 CTA는 고정 하단 탐색(772–834px)보다 충분히 위에 유지된다.

GitHub Pages 최신 배포에서도 수요일 탭을 선택한 `#today` 화면에서 주간 제목·7일 탭·`TODAY'S DELIVERY 03`이 같은 밝은 편지 해변 배경 위에 연속 표시되는 것을 확인했다. 제목 상자 없이도 짙은 청록색 글자와 반투명 아이보리 탭의 대비가 유지됐으며, 배포된 Liblouis 점역 상태도 정상적으로 표시됐다.

## 소개·학습 정원 카드 안전 영역

동일한 Chromium 세션에서 소개 짝 카드, 학습 정원 카드와 7일 탭의 실제 DOM 경계를 측정했다. 데스크톱은 소개 문구 카드와 나비 장면 카드가 모두 564×436px이고, 4개 학습 정원 카드도 모두 581×436px이다. 모바일 390px에서는 소개 짝 카드가 모두 343×384px, 4개 학습 정원 카드가 모두 343×366px로 통일된다. 두 화면 모두 문서·카드의 `scrollWidth`가 `clientWidth`를 넘지 않아 가로 넘침이 없었다.

| 확인 항목 | 데스크톱 1280px | 모바일 390px |
|---|---|---|
| 소개 짝 카드 | 564×436px, 높이 동일, 가로 넘침 없음 | 343×384px, 높이 동일, 가로 넘침 없음 |
| 학습 정원 4개 카드 | 모두 581×436px, 2×2 그리드 | 모두 343×366px, 1열 그리드 |
| 7일 탭 | 7개 중 선택 1개·roving tab stop 1개 | 같은 탭 상태 유지 |
| 키보드·포커스 | 첫 탭에 실제 초점, 3px solid 포커스 윤곽 | 같은 3px solid 포커스 윤곽 |
| 키보드 회귀 | `ArrowRight`·`Home`·`End`가 선택·초점·탭패널을 함께 변경하는 단위 테스트 통과 | 동일 컴포넌트 회귀 테스트 통과 |

모바일 전체 화면에서도 소개 문구·CTA와 나비 장면은 같은 폭·높이의 연속 카드로, 네 개 학습 정원 카드는 동일한 높이로 렌더링되는 것을 확인했다. 이 변경 뒤 전체 23개 테스트 파일·69개 테스트, TypeScript 검사와 Vite 프로덕션 빌드를 다시 통과했다.

GitHub Pages 최신 배포에서도 소개 영역은 밝은 바다빛 배경 위의 같은 크기 텍스트·나비 장면 짝 카드로, 학습 정원은 밝은 2×2 동일 크기 카드로 표시되는 것을 확인했다. 카드 문구는 이미지 위 하단 안전 영역에 남고, 배경에 묻히거나 가로로 넘치지 않았다.

밝은 색조 보정 뒤에는 소개 문구 카드의 어두운 청록 전경(`#053b57`)과 밝은 아이보리·민트 배경(`#f7fffa`)의 대비를 계산해 **11.66:1**로 확인했다. 학습 정원 카드의 흰 문구는 이미지가 가장 밝다고 가정한 상태에서도 두 단계의 하단 읽기 그라데이션을 합성한 배경 `rgb(34, 84, 104)`과 **8.28:1**의 대비를 유지한다. 따라서 대형 불투명 텍스트 패널을 되살리지 않으면서도 일반 본문과 제목에 필요한 대비 기준을 넘긴다.

마지막으로 GitHub Pages의 최신 `main` 배포에서 [`#how`](https://baekjunjoo.github.io/sensory/?lesson=d3#how)와 [`#curriculum`](https://baekjunjoo.github.io/sensory/?lesson=d3#curriculum)을 각각 다시 열어 확인했다. 소개 구간은 밝은 아이보리·민트 문구 카드와 나비 3D 장면이 균형 있게 표시됐고, 학습 정원은 밝은 바다빛 배경 위에 2×2 동일 크기 카드와 하단의 선명한 제목·설명이 유지됐다. 공개 화면의 3D 자산·7일 탭·Liblouis 상태도 정상 표시됐다.

대비 보정이 포함된 최신 공개 배포에서도 소개 카드의 밝은 아이보리·민트 바탕과 짙은 청록 문구, 학습 정원의 밝은 3D 장면과 하단 흰 문구를 다시 확인했다. `#today`의 수요일 학습지에서는 점토 봉투 해변, 촉각 목표, 학습지 열기 CTA와 7일 탭의 선택 상태가 기존 흐름을 유지했다.

후순위 접근성 스타일에 남아 있던 진청 오버레이·복사 상자 규칙은 밝은 `#today`·소개·학습 정원 섹션을 다시 어둡게 만들 수 있어 제거했다. 이어 개발 화면에서 계산 스타일을 다시 확인한 결과, 소개 카드에는 `linear-gradient(rgba(247, 255, 250, 0.94), rgba(205, 240, 239, 0.9))`, 24px 모서리, 42px 내부 여백, `rgb(5, 59, 87)` 문구색이 실제 적용됐다. 학습 정원 카드에는 하단 `rgba(4, 65, 88, 0.82)` 읽기 그라데이션, 탭에는 아이보리 `rgba(255, 253, 244, 0.82)` 또는 선택 상태 하늘색 `rgb(188, 238, 250)`과 짙은 글자 `rgb(32, 32, 30)`가 적용됐다. 회귀 테스트는 이 후순위 덮어쓰기 방지 규칙까지 포함해 70개를 통과했다.

GitHub Pages 최신 `main` 배포에서도 같은 계산 스타일을 다시 확인했다. 소개 카드의 실제 배경은 `linear-gradient(rgba(247, 255, 250, 0.94), rgba(205, 240, 239, 0.9))`, 모서리는 24px, 제목 전경은 `rgb(5, 59, 87)`이며, 7일 탭은 아이보리 `rgba(255, 253, 244, 0.82)` 위 `rgb(32, 32, 30)` 글자로 표시됐다. 이 공개 계산값은 앞서 기록한 소개 카드 11.66:1, 학습 정원 카드 최악 조건 8.28:1의 대비 계산과 함께 밝은 장면의 읽기 기준을 확인하는 근거다.

7일 탭은 같은 산식으로 다시 계산했다. 기본 탭은 가장 불리하게 순검정 배경이 뒤에 있다고 가정해도 반투명 아이보리 표면이 `rgb(209, 207, 200)`이 되어 `rgb(32, 32, 30)` 글자와 **10.47:1**, 선택된 수요일 탭은 하늘색 `rgb(188, 238, 250)` 위에서 **13.02:1**의 대비를 갖는다. 따라서 소개 카드·학습 정원 카드·기본/선택 탭 모두 일반 텍스트 AA 기준을 넘긴다.

### 도착 편지 장면 재구성 진행 기록

가로형 점토 봉투 해변 자산을 새로 연결한 개발 화면에서 기존 주간 소개·7일 탭이 그대로 유지되고, 그 아래 도착 장면에 밝은 해변과 우측 봉투 이미지가 로드되는 것을 확인했다. 다음 검증에서는 문구·CTA와 큰 봉투의 실제 경계, 모바일 세로 장면의 고정 하단 탐색 여백을 별도로 측정한다.

가로형 장면은 좌측의 비어 있는 하늘·파도·모래를 문구 안전 영역으로, 우측의 큰 크림색 점토 봉투·학습지를 시선 초점으로 구성했다. 개발 화면의 실제 경계에서 도착 장면은 데스크톱 `1180×520px`이고, 문구 묶음은 `425px` 폭의 좌측 영역(`left: 107px`), 제목·촉각 목표·CTA는 각각 `1566–1679px`, `1779–1799px`, `1818–1864px`로 서로 겹치지 않았다.

모바일은 별도의 `9:16` 장면을 사용해 문구와 CTA를 위쪽 밝은 하늘 영역(`top: 116–417px`)에 두고, 큰 봉투·학습지는 그 아래 모래 해변에 배치했다. 제목·촉각 목표·CTA는 각각 `156–239px`, `332–352px`, `371–417px`로 상호 비겹침이며, CTA는 고정 하단 탐색 시작점(`772px`)보다 `355px` 위에 있어 조작을 가리지 않는다. 전체 장면 배경이 하단 탐색의 page-coordinate 범위와 겹치는 것은 장면의 비대화형 모래 장식뿐이며, 모든 상호작용 요소는 안전 영역 밖에 있다.

새 장면 적용 후 전체 `23개` 테스트 파일·`71개` 테스트, TypeScript 일회성 검사와 Vite 프로덕션 빌드를 통과했다.

`scrollIntoView({ block: 'start' })` 상태의 실제 캡처에서도 의도한 구성이 확인됐다. 데스크톱 `1280×720px`에서는 밝은 좌측 해변 위에 `TODAY'S DELIVERY 03`·제목·설명·촉각 목표·CTA가 놓이고, 우측에는 크림색 대형 점토 봉투와 학습지가 장면 너비의 약 절반을 차지한다. 봉투는 문구의 우측부터 시작해 두 덩어리가 겹치지 않는다.

모바일 `390×844px`에서는 7일 탭 아래에 문구·촉각 목표·CTA가 먼저 나타나고, CTA 바로 아래부터 점자 촉각 문양 학습지와 큰 크림색 봉투가 이어지는 세로 흐름을 확인했다. CTA 경계는 `top: 380px`, `bottom: 426px`, 고정 하단 탐색은 `top: 772px`이므로 **346px의 안전 간격**이 있다. 고정 탐색과 겹치는 것은 봉투 하단 이후의 모래 배경 일부뿐이며, 포커스 가능 버튼·탭·문구는 모두 그 위에 있어 조작이나 읽기를 방해하지 않는다.

가로형 장면은 최종적으로 독립 사각 카드 대신 투명 배경의 크림색 점토 봉투·점자 봉인·학습지 오브젝트를 사용했다. 실제 `1280×720px` 캡처에서 봉투는 우측 해변 위에 프레임 없이 놓이고, 제목·설명·촉각 목표·CTA는 좌측의 밝은 하늘·바다 여백 위에 남는다. 배경 속 보조 봉투가 보이지 않도록 해변을 좌측 기준으로 확대해, 화면에는 한 개의 큰 점토 봉투만 초점으로 남겼다.

최종 DOM 측정에서 데스크톱 봉투 오브젝트의 실제 경계는 `left: 636px`, `right: 1118px`, `top: 58px`, `bottom: 540px`(`482×482px`)이었다. 좌측 문구 묶음은 `left: 107px`, `right: 531px`, CTA는 `left: 107px`, `right: 259px`이므로 문구와 봉투, CTA와 봉투 모두 **겹치지 않는다**. 이는 가로형 해변의 좌측 텍스트 안전 영역·우측 봉투 초점을 실제 렌더링 결과로 확인한 값이다.

모바일은 독립 오브젝트 대신 세로형 점토 봉투·학습지 해변을 배경으로 사용한다. 실제 `390×844px` 캡처에서 문구·CTA가 위쪽에, 점자 학습지와 봉투가 아래쪽에 나타나는 세로 흐름을 확인했으며, CTA는 `top: 377px`, `bottom: 423px`, 모바일 dock은 `top: 772px`, `bottom: 834px`이다. 따라서 CTA와 모든 포커스 가능 요소는 dock보다 **349px 이상** 위에 있고, 겹치는 범위는 세로형 배경의 하단 모래 장식에만 한정된다.
