# 배경 우선 히어로 레이아웃 검증 기록

## 변경 범위

2026-08-27에 사용자 요청에 따라 모바일 히어로의 제목·설명·CTA·친구 선택 뒤에 있던 큰 진청 배경 패널을 제거했다. 텍스트는 섬 장면 위에 직접 배치하고, 그림자만으로 최소한의 형태 분리를 유지한다. CTA 자체의 조작 표면과 상단·하단 탐색은 유지했다.

## 화면 검토 결과

| 화면 크기 | 확인 결과 |
|---|---|
| 375×812 | 제목은 세 줄로 안정적으로 줄바꿈되며, 설명·두 개 CTA·친구 프로필이 순서대로 이어졌다. 친구 이름 아래와 고정 하단 탐색 사이에 여백이 남고 서로 겹치지 않았다. |
| 390×844 | 장면 라벨, 제목, 본문, CTA, 친구 선택이 하나의 수직 흐름으로 보이며, 하단 탐색은 별도 영역을 유지했다. 큰 텍스트 배경 패널은 표시되지 않았다. |
| 1280×720 | 큰 제목과 보조 문구, CTA, 친구 선택이 섬 장면의 오른쪽 안전 영역에 배치됐고, 상단 고정 헤더와 콘텐츠가 겹치지 않았다. |

## 회귀 보호

`client/src/lib/heroSceneLayout.test.ts`는 히어로 복사본의 투명 배경·패널 제거와 전역 포스터 CSS 뒤에 로드되는 순서를 검증한다. 모바일 규칙은 콘텐츠 묶음을 하단에서 `152px` 위에 두어 고정 탐색과 친구 선택 영역 사이의 여백을 유지한다.

> 사용자는 시각적 패널을 제거하는 선택을 WCAG 대비보다 우선했다. 따라서 이 변경은 큰 대비 패널을 복구하지 않으며, 텍스트 그림자와 장면의 안전 영역으로 가독성을 보조한다.

## Bounding box 기반 비겹침 판정

실제 Chromium 개발 브라우저에서 `getBoundingClientRect()`로 측정했다. 세 화면 모두 히어로 복사본의 배경 계산값은 `rgba(0, 0, 0, 0)`으로, 큰 글자 배경 패널이 제거된 상태다.

| 화면 크기 | 헤더 하단 → 제목 상단 | 제목 → 본문 | 본문 → CTA | CTA → 친구 선택 | 친구 선택 → 모바일 dock | 판정 |
|---|---:|---:|---:|---:|---:|---|
| 1280×720 | 78px | 15px | 18px | 8px | 해당 없음 | 모든 연속 요소가 양수 간격이며 겹침 없음 |
| 390×844 | 192px | 17px | 21px | 11px | 78px | sticky 헤더와 고정 dock 모두 겹침 없음 |
| 375×812 | 167px | 17px | 21px | 11px | 78px | sticky 헤더와 고정 dock 모두 겹침 없음 |

데스크톱에서 최초 발견된 CTA와 친구 선택의 `-7px` 겹침은 `margin-top: 8px`로 보정해 재측정 결과 `8px` 간격으로 해소했다.
