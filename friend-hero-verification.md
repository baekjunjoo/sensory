# 메인 친구 프로필과 히어로 배경 전환 검증 기록

2026-08-27 개발 미리보기의 데스크톱 1280×720에서 히어로 제목의 `피어나는` 밑줄 오프셋을 10px로 적용해 글자와 장식선 사이의 여백을 확인했다. 오늘의 친구 선택 영역은 장면 크롭을 제거한 원형 색상 캐릭터 얼굴 프로필과 이름의 세로 배치로 표시되며, 선택된 항목은 밝은 테두리와 비색상 상태(`aria-pressed`)를 함께 제공한다.

피오 프로필을 선택한 뒤 전환 완료 시점에 `aria-pressed="true"`가 피오에 적용됐고, 히어로 장면 레이블은 `피오 · 촉각 우편 만`, 장면 CSS 변수는 `tuesday-words_fab7d8e3.png`로 바뀌는 것을 확인했다. 친구 선택은 현재 학습지와 독립적으로 히어로 장면만 부드럽게 전환하며, 모션 감소 환경에서는 즉시 전환하도록 구현했다.

모바일 390×844에서 각 친구 프로필은 원형 안에 눈·동공·입이 보이는 색상 얼굴로, 이름은 프로필 아래에 배치되는 것을 확인했다. `피어나는`의 장식선은 본문 글리프에서 분리되어 표시되며, 친구 프로필·이름·하단 빠른 탐색은 겹치지 않고 표시된다.

데스크톱 1280×720에서는 프로필 크기와 상단 간격을 조정하고, 히어로 뒤 접근성 패널의 음수 상단 여백을 홈에서만 제거했다. 그 결과 네 친구의 얼굴과 이름이 모두 히어로 안에 남으며 접근성 패널과 겹치지 않는다.

데스크톱 DOM 경계 측정(1280×1100 뷰포트)에서 히어로는 `top:80px, bottom:1102px`, 접근성 패널은 `top:1102px`로 바로 다음 흐름에 배치됐다. 친구 프로필 버튼은 모두 `top:662px, bottom:724px`, 원형 얼굴은 `46×46px`이었으며 히어로 내부에 완전히 포함됐다. 히어로의 `scrollWidth > clientWidth`는 `false`여서 가로 넘침도 없었다.

회귀 방지 테스트는 데스크톱 폭 1280px에서 피오, 모바일 폭 390px에서 나비를 각각 선택해 `aria-pressed="true"`, 버튼 내 하단 이름, 히어로 장면 레이블 변경을 검증한다. 프로필 CSS 테스트는 원형 얼굴, 프로필-이름의 세로 순서, 모바일 48px 프로필 크기와 홈에서 접근성 패널의 음수 여백을 제거한 규칙을 확인한다. 최종적으로 15개 테스트 파일의 44개 테스트, 타입 검사, 프로덕션 빌드를 통과했다.

실제 390×844 iframe 뷰포트로 DOM 경계를 추가 측정했다. 히어로는 `top:68px, bottom:846px, width:375px`, 접근성 패널은 `top:846px, width:343px`으로 다음 문서 흐름에 배치됐다. 모모·피오·루루·나비 프로필 버튼은 모두 `48×63px`이며 원형 얼굴은 `48×48px`, 이름을 포함한 버튼의 하단은 `772px`로 히어로 내부에 있다. 히어로와 문서 전체의 가로 넘침은 모두 `false`였다.

체크포인트 `52797114`는 GitHub `main`의 커밋 `527971140c14cc1407aa89b7cf99ac567d1ad6e6`으로 동기화됐다. GitHub Pages 배포 실행 `33046442677`은 성공으로 완료됐다.
