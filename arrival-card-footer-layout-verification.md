# 전면 편지·카드·하단 마감 검증 기록

## 전면 오늘의 학습지

첨부 레퍼런스의 핵심 구성을 적용해 오늘의 학습지의 **전체**를 밝은 청록 해변과 대형 크림색 점토 편지 장면으로 전환했다. 데스크톱 `1280×720px` 화면에서 좌측에는 `TODAY'S DELIVERY`·제목·설명·촉각 목표·`학습지 열기` CTA가, 우측에는 열린 편지지와 점자 봉인이 표시된다. 기존의 별도 투명 봉투 이미지는 숨겨 중복 초점이 생기지 않게 했다.

모바일 `390×844px` 화면은 문구·촉각 목표·CTA를 우선 읽은 뒤 하단에 큰 편지와 해변이 이어지는 세로 구성이다. CTA는 편지 장면 위쪽에 남아 있어 고정 하단 탐색과 겹치지 않으며, 문구는 밝은 반투명 그라데이션 안에서 읽을 수 있다.

## 캐릭터 카드와 하단

문해·수학·영어·촉각 그림 카드는 각자 다른 무캐릭터 3D 점토 배경을 사용하고, 기존 주인공 3D 캐릭터는 카드 중앙 상단에 별도 배치했다. 요일·제목·설명은 하단 읽기 그라데이션 영역에 고정해 배경 또는 캐릭터와 겹치지 않는다. `1280×720px` 및 `390×844px` 전체 화면에서 네 카드의 캐릭터 중복·문구 가림·가로 넘침이 없는 것을 확인했다.

하단은 ‘내일’ 안내를 푸터 상단의 간결한 마감 행으로 바꾸고, 그 아래에 DotPad 공식 문의 카드와 브랜드 푸터를 순서대로 배치했다. 데스크톱은 안내·보호자 리포트 링크를 같은 기준선에 두고, 모바일은 충분한 세로 간격의 한 열로 전환한다.

## 자동 검증

`pnpm test && pnpm exec tsc --noEmit && pnpm build`를 실행해 **23개 테스트 파일·74개 테스트**와 TypeScript 검사·Vite 프로덕션 빌드가 통과한 것을 확인했다. Vite의 외부 `/manus-storage` 해석 및 청크 크기 경고는 기존 비차단 경고다.

## 전면 편지 안전 영역 재측정

최신 `16:9` 열린 점토 편지 자산을 연결한 뒤 `#today` 직접 이동 상태에서 DOM 경계를 재측정했다. 데스크톱 `1280×720px`에서 sticky 헤더는 `0–80px`, 도착 장면은 `96–759px`으로 **16px의 여백**이 있어 더 이상 가려지지 않는다. 좌측 제목은 `x:107–470px, y:299–412px`, 촉각 목표는 `y:512–532px`, CTA는 `x:107–259px, y:551–597px`으로 모든 조작·문구 영역이 서로 분리된다.

모바일 `390×844px`에서는 sticky 헤더 `0–68px`, 도착 장면 `104px`부터, 제목 `y:178–261px`, 촉각 목표 `y:354–374px`, CTA `y:393–439px`으로 측정됐다. CTA는 하단 dock 시작점 `772px`보다 `333px` 위에 있고, 도착 장면과 dock이 page-coordinate로 겹치는 부분은 배경 이미지뿐이다. `scroll-margin-top: 96px`과 기존 모바일 scroll margin을 함께 적용해 직접 이동·고정 탐색 상태 모두에서 문구와 CTA의 가림을 방지했다.

## 공개 반영 기준점

2026-08-28 공개 GitHub Pages `?lesson=d3#today` 확인에서는 기존 배포본의 주간 탭·도착 CTA·하단 문의 링크가 모두 로드됐다. 이 시점의 탐색 결과는 새 레퍼런스형 전면 편지 자산을 포함한 로컬 체크포인트 이전 기준이므로, 다음 공개 확인에서는 새 큰 열린 편지·카드별 중앙 캐릭터·정돈한 하단 마감이 포함됐는지 별도로 재확인한다.

## 최신 GitHub Pages 공개 확인

최신 Pages 배포(`https://baekjunjoo.github.io/sensory/?lesson=d3#today`)에서 주간 탭 아래 오늘의 학습지에 **좌측 실시간 문구·CTA와 우측의 대형 열린 크림색 편지·점자 봉인**이 표시되는 것을 확인했다. `#curriculum` 앵커도 최신 CSS에서 학습 정원 제목 위치까지 정상 이동했고, 이어지는 다음 확인에서 카드 네 장과 하단 마감을 별도 확인한다.

공개 `#curriculum` 화면의 영어·촉각 그림 카드에서도 중앙 상단의 루루·나비가 각 카드의 전면 주인공으로만 표시되고, 제목·설명은 하단 읽기 그라데이션 안에 분리되어 있는 것을 확인했다. 배경에는 다른 캐릭터가 없고, 두 카드의 문구·주인공·배경은 겹치지 않는다.

하단 마감 보정 뒤 개발 화면의 `1280×720px` 전체 캡처에서는 ‘내일도 새로운 한 장을 만나.’ 제목이 좌측 안전 여백부터 온전히 시작하고, 우측 보호자 리포트 버튼·DotPad 문의 카드·브랜드 푸터가 같은 좌우 기준선에 맞춰졌다. `390×844px` 전체 캡처에서도 제목·버튼·문의 CTA가 한 열로 전환되며 화면 가장자리에서 잘리지 않았다.
