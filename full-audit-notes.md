# Sensory 전수 점검 기록

## 1차 정적·데스크톱 점검

2026-08-27 기준 `pnpm test`는 16개 테스트 파일·36개 테스트를 통과했고, `pnpm check`와 `pnpm build`도 통과했다. 빌드에는 정적 3D 자산의 런타임 URL 처리, DotPad SDK의 선택적 Liblouis 자산 경로, 500kB를 넘는 초기 JavaScript 번들에 대한 경고가 남아 있으나 빌드 실패나 런타임 오류는 발생하지 않았다.

홈과 보호자 리포트의 데스크톱 전체 화면을 점검했다. 홈의 3D 히어로, 접근성 패널, 주간 학습지, DotPad 연결 패널, 카드형 3D 과목 장면, Liblouis 점자 실험실과 푸터가 모두 렌더링됐다. 보호자 리포트의 히어로·주간 흐름·진행률·영역별 현황·추천·최근 성취도 정상 렌더링됐다. 개발 로그의 과거 Super Dot import 실패와 Liblouis 실행 오류는 초기화 중 남은 이력이며, 가장 최근 HMR·브라우저 로그에는 새 오류가 없다. 공개 로그에는 시작 및 인증 미로그인 이벤트만 남아 있다.

## 발견·수정 대상

DotPad 연결 메서드가 기기 객체를 반환하더라도 연결 콜백이 지연되면 UI가 `연결 중` 상태에 머무를 수 있어 반환값으로도 연결 상태를 확정하도록 보완했다. 또한 Super Dot 정지 함수의 브라우저 객체 접근을 보호해 비브라우저 환경에서 안전하게 종료되도록 수정했다. 실제 음성 출력 장치와 DotPad 320 장비가 없어 음성 품질·실물 촉각 전송은 사용자 환경 검증이 남아 있다.

## 2차 모바일 화면 점검

390×844 화면에서 홈의 모바일 헤더, 히어로 문구·CTA·친구 프로필, 접근성 읽기 패널, 주간 학습지, DotPad 연결 패널, 3D 커리큘럼 카드, 점자 실험실 및 하단 탐색이 한 열 흐름으로 표시됐다. 카드·패널의 가로 넘침과 비정상 겹침은 보이지 않았으며, 고정 하단 탐색은 화면 내에서 별도의 안전 영역을 유지했다.

모바일 보호자 리포트에서도 요약 지표, 7일 학습지 흐름, 영역별 학습 현황, 다음 제안, 최근 성취가 세로 순서로 표시되고 하단 탐색과 겹치지 않았다. 7일 흐름의 각 날짜 타일은 시각적으로 좁지만, 실제 터치·키보드 상호작용과 학습 완료 상태 연동은 다음 기능 점검 단계에서 별도로 확인한다.

## 3차 상호작용 점검

수요일 학습지에서 정답 `5`를 선택했을 때 선택 버튼은 `aria-pressed="true"`와 `picked` 상태를 받고, 정답 확인 버튼이 활성화됐다. 정답 확인 뒤에는 완료 안내 `와, 맞았어! 루루의 볼처럼 자신감이 활짝 피었어!`, 완료 카드, `SUNSET COMPLETE` 히어로 상태가 모두 표시됐다. 따라서 정답 선택·완료 피드백·학습 진행 시각 상태는 개발 브라우저에서 정상 동작했다.

접근성 패널에서 English와 Español을 선택했을 때 각 언어에 맞는 읽기 문장이 즉시 갱신되고, 기존 음성 재생을 멈췄다는 라이브 상태 안내가 표시됐다. 발화 장치가 없는 자동 브라우저에서는 음향 품질을 확인할 수 없지만, 언어 선택·문장 교체·정지 상태는 정상 동작했다. 실제 음성 출력 및 설치된 스페인어 음성 선택은 사용자 브라우저에서 최종 확인이 필요하다.

점자 실험실에 `감각`을 입력하면 Liblouis 요청이 성공해 입력값과 촉각 셀이 갱신됐다. 최초 점검에서 스크린 리더 라이브 영역은 이전 ‘갱신 중’ 문구를 계속 보유했으나, 성공·실패 콜백에서 결과 안내를 갱신하도록 수정한 뒤 초기 화면에서 `Liblouis 표준 점역 결과가 준비됐어요.`가 표시됐다. 로컬 `.txt` 파일을 업로드하면 파일명·문장·`서버로 전송되지 않아요.` 안내와 읽을 영역 옵션이 정상 표시됐다. Tab 키는 첫 번째 초점을 `오늘의 학습지로 바로가기` 링크로 이동시키고 Enter 키는 `#today`로 이동했다.

공용 tRPC CORS는 Piper 전용 모듈·환경 변수 명칭을 `publicApiCors`·`SENSORY_PUBLIC_API_ALLOWED_ORIGIN`으로 정리해 Super Dot과 Liblouis 공용 API 성격에 맞췄다. GitHub Pages 기본 출처만 허용하고, 상태 확인을 위한 GET·점역 POST·OPTIONS를 명시적으로 허용한다.

## 4차 독립 진단 보완

독립 진단 결과를 실제 코드와 비교해 확인했다. GitHub Pages의 외부 Liblouis API 경로는 유지하되, 클라이언트 변수는 `VITE_PIPER_API_URL`에서 `VITE_SENSORY_API_URL`로 전환했고 워크플로는 기존 `PIPER_API_URL` GitHub 변수 값을 새 빌드 변수로 전달하도록 변경했다. 로그인 강제 이동은 메시지 문자열이 아니라 tRPC `UNAUTHORIZED` 코드일 때만 실행하도록 보완했다.

보호자 리포트는 `/report` 직접 주소 진입에서 정상 렌더링됐고, 정적 서버도 파일이 없을 경우 `index.html`을 반환해 SPA 직접 새로고침을 지원한다. 리포트 화면은 지연 로딩으로 분리해 홈 초기 JavaScript 청크를 785.45kB에서 754.77kB로 줄였고, 리포트 청크는 31.42kB로 분리됐다. 번들 경고는 여전히 기준 500kB를 넘지만 실제 빌드는 성공했다.

Liblouis 학습지 점역은 효과 정리 함수의 `active` 플래그로 이전 요일 요청의 결과를 무시하고 있어, 코드 검토에서 응답 경합으로 인한 현재 프레임 덮어쓰기는 재현되지 않았다. 다만 점자 실험실의 성공·실패 콜백은 화면 및 스크린 리더의 안내를 최신 상태로 명시적으로 갱신하도록 수정했다.

헤더와 푸터에서 CSS로만 감춰져 있던 점자 마크·`TOUCH & GROW` 태그라인 DOM을 제거해 Sensory 워드마크만 남겼다. 모바일 메뉴에는 `aria-controls`, 상태별 접근성 이름, 열릴 때 첫 링크로의 초점 이동, Escape 닫기와 메뉴 버튼으로의 초점 복귀를 추가했다. 이 동작과 브랜드 DOM은 회귀 테스트로 확인했다. 최신 개발 로그에는 이전 초기화 과정의 import 오류만 남아 있으며, 재시작 후의 브라우저 로그에는 새 오류가 없다.

## 5차 배포 전 회귀 점검

최신 상태에서 16개 테스트 파일의 38개 테스트, TypeScript 검사, 일반 프로덕션 빌드를 통과했다. GitHub Pages와 같은 빌드 조건으로 `VITE_GITHUB_PAGES=true`, `VITE_SENSORY_API_URL=https://sensorytac-v8ekttiz.manus.space`를 주입한 빌드도 통과했고, 생성된 JavaScript에 해당 공용 API 주소가 포함된 것을 확인했다. 보호자 리포트는 지연 로딩된 상태에서도 `/report` 직접 진입으로 정상 렌더링됐다.

GitHub App 토큰에는 Actions variables 조회·변경 권한이 없어 기존 변수 값을 읽거나 `SENSORY_API_URL`을 자동 생성할 수 없었다. 이에 워크플로는 `SENSORY_API_URL`을 우선 사용하되 기존 `PIPER_API_URL`을 호환 입력으로만 읽게 했고, 운영 설정 안내도 갱신했다. 최신 GitHub Pages 배포에서는 새 변수명과 실제 외부 Liblouis API 호출을 다시 확인해야 한다.

공개 운영 도메인에서 `/report?release=e97d6208`로 처음 진입했을 때는 지연 로딩 fallback만 잠시 보였으나, 다음 페이지 상태 확인에서 리포트 전체가 정상 렌더링됐다. 이는 빈 화면이 아닌 비동기 리포트 청크 로드 중 상태였으며, 리포트의 주요 지표·주간 흐름·학습지 복귀 링크가 확인됐다.

GitHub Pages Origin 헤더를 붙인 실제 공개 Liblouis tRPC 배치 요청은 HTTP 200, `Access-Control-Allow-Origin: https://baekjunjoo.github.io`, `Access-Control-Allow-Methods: GET, POST, OPTIONS`를 반환했다. 응답에는 `engine: liblouis`, `table: ko-g2.ctb`, `locale: ko-KR` 및 `감각`의 4개 점자 셀이 포함됐다. 최신 Pages 번들에는 `https://sensorytac-v8ekttiz.manus.space` 공용 API 주소가 포함된 것도 확인했다.

리포트 지연 로딩이 처음 몇 프레임 동안 배경만 보여 빈 화면처럼 인지될 수 있는 점을 수정했다. 이제 로딩 중에는 `WEEKLY LEARNING REPORT` 라벨과 `보호자 리포트를 불러오고 있어요.` 안내를 표시하며, `aria-busy`와 `aria-live`로 보조기기에도 상태를 전달한다. 수정 뒤 16개 테스트 파일의 38개 테스트, 타입 검사, 프로덕션 빌드를 다시 통과했다. 공개 운영·GitHub Pages 반영 뒤 직접 진입, 새로고침, 복귀 링크를 최종 확인한다.

GitHub Pages 리포트에서는 `/` 절대 링크가 배포 기본 경로(`/sensory/`) 밖을 가리킬 수 있어, 리포트가 런타임의 `BASE_URL`로 홈·학습·점자·리포트 경로를 생성하도록 수정했다. 운영 도메인은 기존 `/`, `/#today`, `/#studio`, `/report` 경로를 그대로 사용한다. 수정 뒤 16개 테스트 파일의 38개 테스트, 타입 검사와 프로덕션 빌드를 다시 통과했다. GitHub Pages 반영 후 실제 클릭·새로고침으로 경로를 확인한다.

최신 GitHub Pages 배포 커밋 `5144c142`에서 `?view=report` 직접 진입을 확인했다. 리포트의 브랜드·뒤로가기 링크는 `/sensory/`, 학습지 링크는 `/sensory/#today`로 생성되어 기존의 배포 루트 이탈 문제가 제거됐다. 첫 렌더는 로딩 fallback에 머무르지 않고 리포트 본문으로 전환됐으며, 자동 브라우저의 링크 클릭은 화면 밖의 요소를 대상으로 해 URL 전환을 관찰하지 못했으므로 동일 경로 직접 진입·새로고침으로 목적지 표시를 추가 검증한다.

최신 GitHub Pages 리포트를 새로고침한 뒤에도 리포트 본문이 정상 렌더링됐고, 학습지 링크는 `/sensory/#today`로 유지됐다. 해당 목적지에 직접 진입해 수요일 학습지와 DotPad 미리보기 영역이 표시되는 것을 확인했다. 이로써 GitHub Pages 기본 경로를 보존한 리포트 직접 진입·새로고침·학습지 복귀 목적지의 표시를 확인했다.

공개 운영 도메인의 `/report?audit=5144c142` 직접 진입에서는 로드 첫 화면에 `WEEKLY LEARNING REPORT`와 `보호자 리포트를 불러오고 있어요.` fallback이 실제 표시됐고, 약 14초 뒤 리포트 본문과 링크가 정상 렌더링됐다. 따라서 지연 청크 로드 중 빈 화면으로 보이던 상태는 명확한 접근성 안내로 보완됐다. 학습지 복귀 링크의 실제 클릭 결과는 별도로 확인한다.

공개 운영 리포트에서 화면 안에 표시된 `오늘의 학습지로 이동` 링크를 실제로 선택했다. 클릭 직후 브라우저 전환 상태가 시작돼 URL·본문 감지가 일시적으로 비어 있었으므로, 이어진 페이지 로드에서 홈 `#today` 도착 여부를 확인한다.

재시도한 공개 운영 리포트의 화면 안에서 `오늘의 학습지로 이동` 링크가 확인됐으며, 링크의 대상은 동일 운영 도메인의 `/#today`다. 다음 단계에서 이 화면 안 링크를 다시 선택해 실제 전환 결과를 확인한다.

전환 과정에서 자동 브라우저 세션이 일시적으로 종료돼 클릭 결과 URL은 직접 수집하지 못했지만, 같은 공개 운영 도메인의 `/#today` 목적지를 다시 열어 수요일 학습지·DotPad 미리보기·`Liblouis 표준 점역 결과가 준비됐어요.` 라이브 안내가 정상 표시되는 것을 확인했다. 최신 서버 진입점은 `publicApiCors`만 import하므로 개발 로그의 `piperCors` 오류는 이미 삭제된 이전 파일을 참조하던 과거 로그로 확인됐다.

재시도한 공개 운영 리포트에서 화면 안의 `오늘의 학습지로 이동` 링크를 실제로 선택했고, 주소가 `https://sensorytac-v8ekttiz.manus.space/#today`로 전환된 뒤 홈의 수요일 학습지·DotPad 미리보기·Liblouis 준비 완료 안내가 정상 렌더링됐다. 이로써 운영 도메인의 직접 진입·로딩 안내·학습지 복귀 클릭 흐름을 확인했다. 또한 공개 홈 화면의 `Liblouis 표준 점역 결과가 준비됐어요.` 라이브 안내로, `VITE_SENSORY_API_URL` 기반 점역 요청의 화면 반영도 확인했다.

공개 운영 리포트에서 `Control+R`을 실제 실행해 새로고침한 뒤에도 페이지는 정상 리포트 본문으로 복구됐다. 앞선 새 세션의 직접 진입에서는 로딩 안내가 먼저 표시되고 본문으로 전환됐으며, 새로고침 후에는 캐시된 리포트 청크가 빠르게 본문으로 전환됐다. 이어 화면 안 학습지 복귀 링크를 선택해 `/#today` 도착과 홈 학습지 렌더링을 확인했으므로, 공개 운영 도메인의 전체 직접 진입·새로고침·로딩·복귀 흐름을 통과했다.

추가 새로고침 검증에서는 공개 리포트를 다시 연 뒤 키보드 새로고침을 실행했다. 본문 전환은 확인됐고, 다음 단계에서 Navigation Timing으로 실제 reload 항목을 수집해 근거를 보강한다.

키보드 새로고침 시도 뒤 Navigation Timing을 조회한 결과는 `type: "navigate"`였다. 자동 브라우저의 키 입력이 실제 reload로 전파되지 않은 것으로 보고, 다음 검증은 페이지의 `location.reload()`를 사용해 실제 reload 항목을 확인한다.

Chrome DevTools Protocol로 새 공개 운영 리포트를 연 뒤 `Page.reload({ignoreCache:true})`를 실행했다. 로드 완료 1초 후 Navigation Timing은 `type: "reload"`, `transferSize: 106396`을 반환했고, `.report-main`은 존재하며 `.report-loading`은 사라졌다. 이는 캐시를 무시한 실제 reload 뒤 리포트 본문 전환이 확인된 근거다. 리포트의 학습지 링크 대상은 `/#today`였다.

최종 CDP 단일 세션 검증에서 리포트 본문이 로드된 뒤 캐시를 무시한 실제 reload를 수행했다. Navigation Timing은 `type: "reload"`, `transferSize: 106396`을 반환했고, reload 중 `.report-loading` 상태가 한 번 이상 관찰된 뒤 `.report-main`이 존재하고 로딩 상태가 사라졌다. 같은 세션에서 학습지 복귀 링크를 클릭하자 URL은 `https://sensorytac-v8ekttiz.manus.space/#today`, 해시는 `#today`가 되었고 학습지 요소가 존재했다. 따라서 공개 리포트의 reload·로딩 안내·본문 전환·학습지 복귀가 연속으로 검증됐다.

최종 단일 세션 검사에서는 리포트 본문이 실제로 로드된 뒤 `Page.reload({ignoreCache:true})`를 실행하고, 다시 `.report-main`이 나타날 때까지 대기했다. Navigation Timing은 `type: "reload"`, `transferSize: 106396`, 로딩 fallback 부재, 리포트 본문 존재를 반환했다. 같은 세션에서 `오늘의 학습지로 이동` 링크를 클릭한 결과 주소는 `https://sensorytac-v8ekttiz.manus.space/#today`로 전환됐고, `#today` 학습지 요소가 존재했다. 공개 운영 리포트의 직접 진입·실제 reload·본문 전환·학습지 복귀 흐름을 연속 검증했다.

개발 서버를 재시작한 뒤 최신 `devserver.log`와 `browserConsole.log`를 다시 확인했다. 서버는 3000 포트에서 정상 기동됐고, 이전 `server/piperCors` 모듈 누락 오류는 새 로그에 재발하지 않았다. 브라우저 콘솔에는 개발 연결·디버그 초기화 항목만 있어, 해당 오류는 현재 코드가 아닌 과거 로그 이력으로 판단했다.
