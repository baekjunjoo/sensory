# 홈 굵은 제목 자간 보정 검증 기록

2026-08-27 개발 미리보기의 데스크톱 1280×720 및 모바일 390×844 화면에서 홈의 표시형 제목 자간을 `-0.085em`에서 `-0.04em`으로 완화한 결과를 확인했다. 히어로 제목의 획과 글자 사이에 여유가 생겼으며, 지정된 줄바꿈, CTA, 캐릭터 영역과 겹치지 않고 가로 넘침도 관찰되지 않았다. 이후 사용자 피드백에 따라 글자 획이 더 분명히 분리되도록 최종 자간을 `0.08em`으로 추가 확장하고 같은 화면 크기에서 재확인한다.

계산 스타일 점검에서 히어로 제목과 성장 포스터·리본 문구가 초기 선택 범위에서 누락된 것을 확인했다. 또한 후속 몰입형 히어로 스타일이 히어로 제목을 `-0.1em`으로 다시 덮어쓰는 것을 발견했다. 최종 보정은 해당 표시형 제목 선택자에만 `0.04em !important`를 적용해 후속 스타일보다 우선하도록 하며, 본문, 내비게이션, 점자 텍스트, 버튼의 자간은 변경하지 않는다.

후속 히어로 규칙을 우선시한 `0.04em` 자간을 데스크톱 1280×720과 모바일 390×844에서 재확인했다. 히어로의 굵은 글리프가 서로 닿지 않고 구분되며, 제목의 응집도와 지정된 줄바꿈도 유지됐다. 화면 양쪽으로 벗어나는 제목은 관찰되지 않았다.

개발 화면의 계산 스타일에서 히어로 제목의 최종 `letter-spacing`은 `3.6864px`이며, 제목 자체의 가로 스크롤 초과와 뷰포트 초과는 모두 `false`임을 확인했다. 다른 대상 제목도 같은 선택자 규칙의 양수 자간을 적용받으며, 본문·탐색·점자 텍스트는 대상에 포함되지 않는다.

최종 데스크톱 계산 스타일 점검에서 대상 선택자 모두 양수 자간과 `scrollOverflow: false`, `viewportOverflow: false`를 반환했다. `h1`은 `3.6864px`, 섹션 제목은 약 `2.36–2.56px`, 포스터·리본·학습 질문 제목은 약 `0.72–1.52px`의 자간이 적용됐다. 모바일 390×844 화면도 같은 제목 선택자의 화면 결과를 확인했으며, 줄바꿈과 가로 넘침은 없었다. `headlineSpacing.test.ts`는 모든 대상 선택자와 `0.04em !important` 값을 회귀 검증한다.

모바일 390×844 폭을 독립 프레임으로 재현해 모든 대상 선택자의 계산 스타일을 다시 점검했다. 히어로 제목은 `2.34px`, 섹션 제목은 약 `1.52–1.92px`, 포스터·리본·학습 질문 제목은 약 `0.72–1.20px`의 양수 자간을 보였고, 각 항목의 `scrollOverflow`와 `viewportOverflow`는 모두 `false`였다.
