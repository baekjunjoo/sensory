# 히어로 서브문구 문단 정렬 검증 기록

히어로의 설명 문구를 `max-width: 35ch` 제약에서 분리해 최대 550px의 균형 문단으로 바꿨다. 데스크톱 1280×720에서는 문장이 서로 떨어진 조각이 아니라 같은 폭의 세 줄 문단으로 정렬되고, 제목 아래에서 CTA 위까지 일관된 간격을 유지한다.

모바일 390×844에서는 화면 폭 안에서 세 줄로 자연스럽게 줄바꿈되며, CTA·오늘의 친구 프로필·하단 고정 탐색과 겹치지 않는다. 지원하는 브라우저에서는 `text-wrap: balance`를 사용하며, 지원하지 않는 브라우저에서도 일반 줄바꿈으로 읽을 수 있다.
